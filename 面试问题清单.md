# 大健康行业智能问诊系统 — 面试问题全清单（详细答案版）

> 以下问题均基于本项目实际代码和技术选型，按面试官视角从浅入深排列。
> 每个问题附带「标准参考回答」和「面试官追问」，覆盖 68 问 × 12 个维度。

---

## 一、项目总体与架构设计

### Q1. 请用 3 分钟介绍一下这个项目。

**标准回答**：

这是一个「大健康行业智能问诊系统」，定位是**预问诊 + 医疗业务管理**的一体化平台。不是替代医生，而是在患者就诊前帮他做症状分析、给出参考建议和就医引导。

技术架构上是 **三层分离**：
- **前端**：React 19 + TypeScript + Vite + TailwindCSS，单页应用，负责用户交互和问诊对话界面
- **后端（Java）**：Spring Boot 3.2 + MyBatis-Plus，负责业务逻辑——用户管理、预约挂号、电子病历、科室管理、问诊会话编排
- **AI 服务（Python）**：FastAPI + LangChain + ChromaDB + DeepSeek，负责 RAG 检索增强生成和思维链推理

三层之间通过 REST API（普通 CRUD）和 SSE（流式 AI 输出）通信。选 Java 做业务层是因为业务逻辑复杂，需要事务、权限、多表联查这些；选 Python 做 AI 层是因为 LangChain 生态全在 Python 侧——这是典型的「LLM Application Engineer」的异构架构思路。

核心亮点：
1. **多轮对话状态管理**：DiagnosisSession + ChatHistory 两张表实现了跨请求的上下文记忆，用滑动窗口 + 摘要压缩策略控制 token 消耗
2. **Spring Security + RBAC**：三角色（患者/医生/管理员）+ 方法级 `@PreAuthorize` + 数据范围过滤（行级安全）
3. **AI 多层降级**：结构化链路 → 容错解析 → 兜底建议，保证 AI 不可用时系统不崩溃

**面试官追问**：「你一个人写的吗？花了多久？」——全栈独立开发。核心功能约 2 周，后续持续打磨。

---

### Q2. 为什么选择 Spring Boot + FastAPI 这种 Java/Python 异构架构？用一个语言写不更简单吗？

**标准回答**：

这个问题问到了异构架构的本质理由。其实核心矛盾是：**企业级业务系统（事务、权限、CRUD）和 AI 应用（LangChain、向量检索、LLM 调用）的最佳生态分别落在不同语言上。**

Java + Spring Boot 的优势：
- Spring Security 的 RBAC 体系非常成熟，`@PreAuthorize` + SecurityContext 能精细到方法级
- MyBatis-Plus 做复杂联表查询（比如「查某科室所有医生 + 关联用户表拿姓名 + 统计预约量」）比 Python ORM 方便得多
- 声明式事务 `@Transactional` 天然适合预约挂号这种「查 → 判 → 插」需要原子性的场景

Python + FastAPI 的优势：
- LangChain 的 LLM 抽象层（`ChatPromptTemplate`、结构化输出）、向量存储统一接口（ChromaDB 切换 Faiss 零代码改）只在 Python 生态完整
- 异步生态（`async/await` + `httpx`）天然适合 I/O 密集的 LLM 调用
- `SSE`（流式输出）在 FastAPI 中几行就能搞定：`StreamingResponse` + `generate()` 生成器

如果全用 Python：RBAC 权限模型得自己造（FastAPI 没有方法级注解），多表联查 ORM 不好写。如果全用 Java：LangChain 的 Java 版（LangChain4j）目前还不成熟，DeepSeek 的 SDK 生态也主要在 Python 侧。

**中间打通方式**：REST + SSE。Java 侧用 `RestTemplate`（或 WebClient）调 Python 的 REST 接口，数据格式统一 JSON（snake_case），SSE 流式响应直接透传给前端。这就是「LLM Application Engineer」的核心工作——不是只写 Python 或只写 Java，而是知道在哪个环节用哪种技术最合适，然后打通它们。

**面试官追问**：「如果有 10 个 Python AI 服务的实例，Java 怎么负载均衡？」——用 Spring Cloud LoadBalancer 或 Nginx upstream，AI 服务无状态所以轮询即可。或者把 AI 调用抽成一个独立的 `@FeignClient`。

---

### Q3. 前后端如何通信？有哪些协议？为什么不用 WebSocket？

**标准回答**：

本系统用了两种通信协议：

1. **REST over HTTP（JSON）**：普通 CRUD 操作——登录注册、科室/医生查询、预约管理、病历管理。请求-响应模式，无状态，适合增删改查。
2. **SSE（Server-Sent Events）**：AI 问诊流式输出——用户输入症状，Java 转发到 Python AI，Python 一边生成 token 一边通过 SSE 推回给 Java，Java 透传给前端。单向推送（服务器 → 客户端）。

为什么选 SSE 而不是 WebSocket：
- **SSE 更轻量**：基于 HTTP 协议，不需要升级握手，不需要心跳保活。LLM token 流式输出场景是单向的（AI 推 token 给前端，前端此时不需要再发消息），SSE 足够。
- **天然支持断线重连**：`EventSource` API 会在连接断开后自动重连，且带着 `Last-Event-ID` 从断点续传。WebSocket 断线后需要自己实现重连逻辑。
- **穿透代理/防火墙友好**：SSE 就是普通 HTTP 长连接，不需要像 WebSocket 那样额外配置 Nginx 的 `Upgrade` 头。

什么时候会用 WebSocket：如果未来加了「医生-患者实时聊天问诊」功能，需要双向即时通讯，那就会引入 WebSocket。本系统的交互模式是「患者问 → AI 答」，不需要双向。

**面试官追问**：「SSE 的数据格式是什么？」——`text/event-stream`，格式为 `data: <json>\n\n`。本系统 Python 端 `StreamingResponse` + `yield f"data: {json.dumps(msg)}\n\n"`。

---

### Q4. 你在项目中担任什么角色？遇到了什么技术挑战？怎么解决的？

**标准回答**：

全栈独立开发，从数据库设计到前后端到 AI 集成都是自己做的。最大挑战有三个：

**挑战一：多轮对话状态如何跨请求保持。** 问诊不像查资料——病人说完症状，AI 得追问，一来一回好几轮。我需要让每一次 HTTP 请求都能「记住」前面的对话。解决方案是两层状态管理：`DiagnosisSession` 表（用 UUID 作为 sessionKey 唯一标识一次完整问诊）+ `ChatHistory` 表（按 sessionId 分组，每条消息标 role 和时间）。前端 localStorage 持久化 sessionKey，每次请求都带上，服务端就能串起整个对话链。同时还做了滑动窗口（最近 10 条）+ 摘要压缩（超 20 条用摘要替代），控制 token 消耗。

**挑战二：如何把 Spring Security 无缝嵌入已有的自定义 JWT 认证。** 一开始用的是自定义 `JwtInterceptor` + Hutool BCrypt，只能做「登录了 / 没登录」的二元判断，没法做「这个接口只能医生调」。改为 Spring Security 后，关键设计是「双写模式」——`JwtAuthenticationFilter` 中，认证结果同时写入 `SecurityContext`（给 `@PreAuthorize` 方法级注解用）和 `UserContext`（ThreadLocal，给业务代码 Service 层做数据范围过滤用）。两者职责分离。

**挑战三：AI 服务不可用时的降级策略。** Python 端做了三层容错：结构化链路（PydanticParser 解析）失败 → 原始输出 + 正则容错解析 JSON → 兜底建议（返回「就医建议」+ 免责声明）。Java 端用 `CompletableFuture.supplyAsync` + 独立线程池 + 超时机制，AI 超时不阻塞 Tomcat 主线程。

**面试官追问**：「你遇到的最大 bug 是什么？」——多轮对话编码时，`buildHistoryText()` 里 role 标签拼接逻辑有个 off-by-one 错误：`[第0轮 - 用户]` 应该从 1 开始编号。原因是 `(i / 2)` 当 i=0 时得 0。改成 `(i / 2) + 1` 后修正。

---

### Q5. 前端 React 18 还是 19？用的什么构建工具？为什么选 Vite？

**标准回答**：

React 19 + TypeScript + Vite + TailwindCSS。

核心状态管理没有用 Redux 或 Zustand——项目规模不大，`useState` + `useEffect` + `useRef` 够用。多轮对话的状态（messages 数组、sessionKey、loading）都在 `ConsultationPage` 组件内部管理，sessionKey 通过 `localStorage` 持久化做跨页面刷新保持。

选 Vite 的原因是：开发体验好——HMR（Hot Module Replacement）秒级热更新，改一个组件不需要重新打包整个应用。CRA（Create React App）基于 Webpack，开发服务器启动要几十秒，Vite 基于 esbuild 预构建依赖 + 浏览器原生 ESM，1-2 秒就起。

TailwindCSS 选它是因为原子化 CSS 写组件太高效了——不需要在 `.css` 文件和 `.tsx` 之间来回切换，className 即样式。聊天气泡、表单卡片、状态标签这些都是在 JSX 里直接写 `flex gap-2 p-4 rounded-lg bg-blue-50`。

**面试官追问**：「如果项目扩大，会话数变多，你会怎么管理状态？」——引入 Zustand（轻量级，和 Redux 比 API 简洁很多）做全局 session 管理，多个聊天窗口并存时用 `useSessionStore` 按 sessionKey 索引。

---

### Q6. 介绍一下 Docker Compose 中的服务编排。depends_on 和 healthcheck 的区别？

**标准回答**：

`docker-compose.yml` 编排了 4 个服务：mysql、redis、ai-service、backend。

服务启动顺序用 `depends_on` 控制：`backend` 依赖 `mysql` + `redis` + `ai-service`。这里有个关键细节：**用的是 `condition: service_healthy` 而不是简单的 `depends_on`**。

`depends_on` 不带 condition 时，只保证「容器启动了」——但 MySQL 容器启动后，mysqld 进程可能还在初始化数据目录、还没开始接受 TCP 连接。如果 backend 这时候启动，数据库连接会失败。

`condition: service_healthy` 则要求 MySQL 的 healthcheck 命令（`mysqladmin ping`）连续返回成功才能算就绪。healthcheck 配置了 `start_period: 30s`（前 30 秒不计失败）、`interval: 10s`（每 10 秒检查一次）、`retries: 10`（失败 10 次才标记 unhealthy）。这样就保证了 backend 启动时 MySQL 一定可以接受连接。

数据持久化方面：mysql、redis、chroma 三个数据服务各自挂载了独立的 named volume：
- `mysql-data:/var/lib/mysql` — 数据库数据不随容器销毁
- `redis-data:/data` — 缓存和排行榜数据持久化
- `chroma-data:/app/data` — 向量知识库存下来，不用每次重建

环境变量用 `${VAR:-default}` 语法提供默认值（如 `MYSQL_ROOT_PASSWORD:-root123`），生产部署时通过 `.env` 文件覆盖。AI 服务还额外通过 `path: ./ai-service/.env` 注入 DEEPSEEK_API_KEY 等敏感配置。

**面试官追问**：「backend 的 depends_on 里 ai-service 为什么是 `service_started` 而不是 `service_healthy`？」——因为 ai-service 的 Dockerfile 里没有定义 healthcheck，所以只能用 `service_started`。但 backend 侧代码有 `CompletableFuture.get(timeout)` 的超时重试降级，即使 AI 服务还没完全就绪，问诊请求也不会雪崩。

---

## 二、Spring Security + JWT + RBAC（最高频，准备透彻）

### Q7. 为什么要把自定义 JwtInterceptor 替换成 Spring Security？原来的方案不是也能用吗？

**标准回答**：

原来的 `JwtInterceptor` 方案有两个致命缺陷：

**1. 只能做认证，做不了授权。** 拦截器只能判断「token 有效 / 无效」——有效的就放行，无效的就拦截。它不知道「当前登录的用户是患者、医生还是管理员」，更做不到「这个接口只能医生调，那个接口只能管理员调」。要在拦截器里实现角色区分，就得在每个 Controller 方法里手写 `if (role != ADMIN) throw ...`，这不符合 AOP 的思想，也会导致权限逻辑散落各处。

**2. 缺失安全基础设施。** 原来的密码用的是 Hutool 的 `BCrypt`（而且调用方式有问题），没有标准的 `PasswordEncoder` 接口抽象。CSRF 防护、会话管理、认证异常处理——这些 Spring Security 提供了一套成熟的框架级能力，自己从头写一遍时间成本高且容易有安全漏洞。

Spring Security 替换后带来的能力：
- `@PreAuthorize("hasRole('ADMIN')")` 一行注解就能实现方法级权限控制
- `PasswordEncoder` 接口——将来想换 Argon2 只需改一个 Bean
- `SecurityContextHolder` 与框架内部机制打通（如 `@AuthenticationPrincipal` 注入当前用户）
- `AuthenticationEntryPoint` 和 `AccessDeniedHandler` 统一处理 401/403，返回 JSON 而非 HTML 重定向

**面试官追问**：「迁移过程中有遇到兼容性问题吗？」——有。原来的 `CorsConfig` 用 `WebMvcConfigurer.addCorsMappings` 注册 CORS，升级到 Spring Security 后需要集成到 `SecurityFilterChain` 的 `.cors()` 中，因为 Security 过滤链在 MVC 之前，Security 的 CORS 配置优先级更高。所以我把 CORS 从独立的 CorsConfig 搬到了 SecurityConfig 里。

---

### Q8. SessionCreationPolicy.STATELESS 是什么意思？为什么关 CSRF？

**标准回答**：

`SessionCreationPolicy.STATELESS` 告诉 Spring Security：不要创建 `HttpSession`，不要用 JSESSIONID Cookie 跟踪用户。每个 HTTP 请求都是独立的，身份信息完全靠请求头中的 JWT token 来识别。

这对于前后端分离架构是必须的——前端和后端可能部署在不同域名/端口下，Cookie 跨域传递有种种限制（SameSite、CORS credentials），而且服务器不需要在内存中维护会话状态，天然支持水平扩展（加一台后端实例，不用同步 session）。

为什么关 CSRF：CSRF（跨站请求伪造）的攻击原理是——攻击者诱导用户在登录状态下访问恶意网站，恶意网站用 `<form>` / `<img>` 等标签自动发请求到目标网站，浏览器会自动带上 Cookie（包括 JSESSIONID），导致「借用户的身份执行操作」。

但 JWT 场景下这个攻击路径不成立：浏览器**不会**在跨域请求中自动附带 `Authorization: Bearer <token>` 头。JWT 必须由前端代码手动从 `localStorage` 取出并拼接到请求中。恶意网站的 JavaScript 既不能读取你的 localStorage（同源策略），也不能让你的浏览器自动发送 `Authorization` 头。所以 JWT + 前后端分离架构下 CSRF 攻击找不到入口，关掉 CSRF 是安全的。

**面试官追问**：「如果你们把 JWT 存在 Cookie 里呢？」——那就需要开 CSRF。Cookie 里的 token 浏览器会自动带，需要 CSRF Token（Double Submit Cookie 模式）来防御。但存 Cookie 有其他问题（跨域限制、HTTPS 要求），所以项目选的是 `localStorage` + 请求头。

---

### Q9. JwtAuthenticationFilter 继承 OncePerRequestFilter 而不是 GenericFilterBean，有什么区别？

**标准回答**：

这是一个很细的 Spring Security 面试点。`OncePerRequestFilter` 是 `GenericFilterBean` 的子类，核心区别在于：

**`GenericFilterBean`**：每次请求经过 `FilterChain` 时都会执行。在 Spring Security 中，一个 HTTP 请求可能经过多次 Filter Chain——比如请求 `forward` 或 `include` 到另一个 Servlet，或者使用了 `RequestDispatcher` 的错误页面。如果 JWT 过滤器被多次执行，就会：
- 多次解析 token（浪费 CPU）
- 多次设置/清理 `UserContext`（可能产生 ThreadLocal 污染）

**`OncePerRequestFilter`**：通过 `request.getAttribute("filterName.FILTERED")` 标记来保证同一个请求只执行一次 `doFilterInternal`。即便请求被 forward/include 多次，过滤器也只在第一次通过时执行。

在我们这个项目中，`SessionCreationPolicy.STATELESS` + 无 JSP/forward 的 REST 架构下，请求不会被 dispatch 多次，所以实际上 `GenericFilterBean` 也不会重复执行。但选 `OncePerRequestFilter` 是**防御性编程**——万一将来有人加了 forward 逻辑，不会因为「过滤器莫名执行了两次」而产生诡异 bug。

**面试官追问**：「`shouldNotFilter` 方法你用了吗？」——没有。当前实现是在 `doFilterInternal` 内判断 token 是否存在再做解析，白名单路径（login/register）压根不会带 token，token 为 null 就直接走 filterChain 放行。如果要跳过特定路径（如静态资源），更优雅的做法是在 `shouldNotFilter` 中返回 true。

---

### Q10. JWT 的 payload 里放了什么？为什么把 role 放 JWT 里而不是每次查数据库？

**标准回答**：

JWT payload 中放了三样：`userId`、`username`、`role`（0/1/2 整数）。

把 role 放 JWT 的核心原因是 **STATELESS** 设计目标——每个请求不需要再查数据库就能完成**授权判断**。来看两条路径的区别：

**方案 A（查数据库）**：`请求 → 解析 JWT 拿 userId → 查数据库 SELECT role FROM t_user WHERE id=? → 判断角色 → 执行操作`。每次需要多一次数据库查询，高并发下数据库压力大。

**方案 B（JWT 携带 role）**：`请求 → 解析 JWT 拿 userId + role → 直接构建 GrantedAuthority → 执行操作`。零数据库查询完成授权。

这在 Spring Security 中尤其关键——`@PreAuthorize("hasRole('ADMIN')")` 的权限判断发生在方法调用之前，过滤器层就要构建好 `Authentication` 对象。如果 role 不在 JWT 里，过滤器就得查数据库，本应轻量的过滤器变成了数据库依赖。

**代价和补救**：JWT 内容不可变，直到 token 过期前拿的都是签发时的 role。如果管理员把一个用户的角色从 PATIENT 升级到 DOCTOR，或者降级用户，旧的 JWT 不会立刻感知到变化。解决方案：
1. JWT 短有效期（如 15 分钟 ~ 1 小时），让旧 token 在合理时间内自然过期
2. 敏感操作（如创建病历、修改知识库）再加一层数据库实时校验
3. 紧急降权场景用 Redis 黑名单（把被降权的 token 的 jti 存入 SET），过滤器里额外查一次

**面试官追问**：「JWT 用的是什么签名算法？」——HMAC-SHA256（`Keys.hmacShaKeyFor` + `Jwts.SIG.HS256`），对称加密。服务端用同一个 secret 签发和验证。生产环境建议 ES256（非对称），公钥可以在多个服务间分发验证。

---

### Q11. 为什么 JwtAuthenticationFilter 里同时写了 UserContext 和 SecurityContext？不是只用 SecurityContext 就行了吗？

**标准回答**：

这是本系统安全架构的一个关键设计决策——**双写模式**。两个 Context 的职责和消费方不同：

| Context | 职责 | 消费者 | 存储内容 |
|---------|------|--------|----------|
| `SecurityContext` | 方法级授权判断 | `@PreAuthorize` 注解 | `Authentication`（userId + GrantedAuthority） |
| `UserContext`（ThreadLocal） | 业务代码拿当前用户信息 | Service 层（如 MedicalRecordServiceImpl、AppointmentServiceImpl） | userId + username + role |

**为什么各有各的用**：

`SecurityContext` 的问题是：从 Service 层拿 userId 很别扭——需要 `SecurityContextHolder.getContext().getAuthentication().getPrincipal()` 还得类型转换。而 `UserContext.getUserId()` 一行搞定，代码简洁。

`UserContext` 的问题是：Spring Security 框架不认识它。`@PreAuthorize` 只看 `SecurityContext` 中的 `Authentication.getAuthorities()`，不知道 UserContext 的存在。如果只写 UserContext、不写 SecurityContext，注解鉴权就失效了。

所以「双写」是实用主义的选择——JwtAuthenticationFilter 中同时写入两个 Context，让注解层和业务层各自用最方便的方式获取用户信息。

**面试官追问**：「数据来源是同一份（JWT token），有没有可能两个 Context 不一致？」——理论上不会，因为在同一个 `doFilterInternal` 方法内，`userId`/`username`/`role` 都是从同一个 JWT 解析出来的，写入两个 Context 使用的是同一份变量。但如果后续代码里手动改了 `UserContext`（比如 `UserContext.setUserId(newId)`），两个 Context 就会不一致。好在项目中没有任何地方手动 Set UserContext 的值，只有 JwtAuthenticationFilter 在写。

---

### Q12. UserContext 用了 ThreadLocal，你如何处理内存泄漏？为什么 ThreadLocal 会内存泄漏？

**标准回答**：

ThreadLocal 内存泄漏的根因是：它的 Entry 继承自 `WeakReference`，Key（ThreadLocal 对象）是弱引用，但 Value（业务数据）是强引用。当 ThreadLocal 对象不再被外部引用时，Key 被 GC 回收，但 Value 仍然存在——因为 `Thread.threadLocals` 表中有到这个 Entry 的强引用链：`Thread → ThreadLocalMap → Entry → Value`。如果线程是线程池管理的（Tomcat 工作线程），线程不会销毁，Value 就一直不能被回收。

更危险的是**业务问题**：Tomcat 默认 200 个工作线程，线程池复用意味着当前请求结束后，下一个请求**可能分配到同一个线程**。如果 UserContext 在上一个请求结束后没被清理，下一个请求的过滤器可能又不带合法 token（或者带了不同的 token），就会出现「读到了上一个请求的用户 ID」的**数据串线**。

我的处理方式是：

在 `JwtAuthenticationFilter.doFilterInternal()` 中用 `try-finally` 保证清理：

```java
try {
    filterChain.doFilter(request, response);
} finally {
    UserContext.clear();                      // 清理业务 ThreadLocal
    SecurityContextHolder.clearContext();     // 清理 Security ThreadLocal
}
```

`finally` 块无论业务代码是否抛异常都会执行。`UserContext.clear()` 里调 `ThreadLocal.remove()`——这比 `set(null)` 更彻底，`remove()` 会从 ThreadLocalMap 中删除整个 Entry，而不是只把 Value 置空。

**面试官追问**：「两个清理的顺序有讲究吗？」——先清理业务 Context 还是先清理 Security Context 没有影响，它们是独立的 ThreadLocalMap Entry。但**必须放在 finally 块**，不能放在 try 块末尾——因为如果业务代码抛了未捕获异常，try 块末尾的代码不会执行。

---

### Q13. `@EnableMethodSecurity(prePostEnabled = true)` 的作用是什么？和 `@Secured` 有什么区别？

**标准回答**：

`@EnableMethodSecurity(prePostEnabled = true)` 是 Spring Security 的方法级安全的总开关。它的参数含义：
- `prePostEnabled = true`：开启 `@PreAuthorize` 和 `@PostAuthorize` 注解支持
- `securedEnabled = true`：开启 `@Secured` 注解支持（旧版，项目中没开）
- `jsr250Enabled = true`：开启 JSR-250 注解（`@RolesAllowed`，项目中没开）

本项目只用 `prePostEnabled`。`@PreAuthorize` 和 `@Secured("ROLE_ADMIN")` 的核心区别：

1. **表达式能力**：`@PreAuthorize` 支持 SpEL 表达式，可以写复杂逻辑。例如：
   - `@PreAuthorize("hasRole('ADMIN')")` — 必须有 ADMIN 角色
   - `@PreAuthorize("hasRole('DOCTOR') and #userId == authentication.principal")` — 必须是医生，而且参数 userId 要和当前登录用户一致（防越权）
   - `@PreAuthorize("hasRole('ADMIN') or hasRole('DOCTOR')")` — 管理员或医生

   `@Secured` 只能写固定角色名列表，不支持 SpEL。

2. **执行时机**（Pre vs Post）：
   - `@PreAuthorize`：方法执行**前**判断。权限不够直接抛 `AccessDeniedException`，方法体不执行，省资源。
   - `@PostAuthorize`：方法执行**后**判断。用于「方法执行完后，拿到返回数据，再判断当前用户是否有权看这个数据」的场景。本项目中没用到但可以实现「用户只能看自己的订单」这种细粒度控制。
   - `@Secured`：只能 Pre，且返回值拿不到。

**面试官追问**：「项目中哪些 Controller 用了 @PreAuthorize？」——KnowledgeController 的 create/update/delete 方法加了 `@PreAuthorize("hasRole('ADMIN')")`，AppointmentController 的 book 方法加了 `@PreAuthorize("hasRole('PATIENT')")`，MedicalRecordController 的 create 方法加了 `@PreAuthorize("hasRole('DOCTOR')")`。

---

### Q14. BCryptPasswordEncoder 和 BCrypt 有什么区别？为什么不用 MD5/SHA256？

**标准回答**：

这个问题考察你对密码哈希本质的理解。关键是「快哈希 vs 慢哈希」的区别。

**MD5/SHA256 是快哈希**：设计目标是快速计算、用于校验数据完整性。MD5 一秒钟能算几十亿次。这在密码存储场景是致命的——攻击者拿到数据库后，可以用 GPU 集群做离线暴力破解：`for each word in dictionary: if md5(word) == stored_hash: got it!`。普通人用的 6-8 位密码，几小时就能遍历完。

**BCrypt 是慢哈希**：专为密码存储设计，核心特征是：
1. **自带盐值（salt）**——每次 hash 随机生成 16 字节 salt，相同的密码两次 hash 结果完全不同，彩虹表攻击失效
2. **计算代价可配置**——BCrypt 有个 cost factor（默认 10，即 2^10 = 1024 次迭代），可以通过调高 cost 来对抗硬件性能提升。cost 增加 1，计算时间翻倍

Spring Security 的 `BCryptPasswordEncoder` 是 BCrypt 算法的标准 Java 实现。关键 API：
- `passwordEncoder.encode(rawPassword)` → 输出 `$2a$10$...`，可以直接存数据库
- `passwordEncoder.matches(rawPassword, encodedPassword)` → 内部提取 salt 重新算一遍然后比对

**Hutool BCrypt vs Spring Security BCryptPasswordEncoder**：两者实现同一算法，但 Spring Security 的 `PasswordEncoder` 是一个接口抽象。它的好处是——将来想换 Argon2（比 BCrypt 更新的算法，抗 GPU 破解更强），只需改一行 Bean 注入代码：

```java
// 今天
@Bean
public PasswordEncoder passwordEncoder() {
    return new BCryptPasswordEncoder();
}
// 明天换 Argon2
@Bean
public PasswordEncoder passwordEncoder() {
    return Argon2PasswordEncoder.defaultsForSpringSecurity_v5_8();
}
```

所有调 `passwordEncoder.matches()` 的业务代码（登录校验、修改密码）一行不用改。

**面试官追问**：「BCrypt 的 cost factor 设多少合适？」——默认 10，hash 一次约 100ms。对登录场景，100ms 是可以接受的（用户不会感知）。如果设到 12（400ms），安全性提高但登录体验下降。生产环境建议用性能测试确定：取「用户体验能接受的最长等待时间」对应的 cost。

---

### Q15. 你的白名单里放了 `/api/ai/health` 和 `/api/medical/hot`，为什么这些不需要认证？

**标准回答**：

白名单设计遵循一个原则：**只对需要「用户身份识别」的接口要求认证，公共/运维接口放行**。

白名单内容：
1. `/api/user/login` + `/api/user/register` — 登录和注册，此时用户还没拿到 token，不可能带认证信息。
2. `/api/ai/health` — AI 服务健康检查。这是 Docker/K8s 的探活接口（liveness probe），kubelet 调它时不带 token。如果要求认证，healthcheck 永远失败 → 容器被持续重启。
3. `/api/medical/hot` — 热门问诊排行榜。类似「首页推荐」，不需要登录就能看。这是产品层面的公共内容，类似微博热搜。如果要求登录才能看，首页就是空白的，用户体验很差。
4. `/error` — Spring Boot 默认错误页面路径。某些异常（如请求路径不存在）会被 Spring 内部转发到 `/error`，如果不放行会导致「404 变为 401」的诡异行为。
5. `OPTIONS /**` — CORS 预检请求。浏览器在发送跨域请求前会先发 OPTIONS 请求询问服务器是否允许。这个请求**不带 Authorization 头**（CORS 规范），如果要求认证，所有跨域请求都会因为预检失败而发不出去。

**面试官追问**：「为什么不把所有 GET 请求都放白名单？」——那就不安全了。GET `/api/user/profile` 是查看个人信息，必须认证。白名单是「逐条审视 + 最小放行」的原则，不是按 HTTP 方法一刀切。

---

### Q16. 如果有用户登录后角色被管理员改了（比如 PATIENT → DOCTOR），JWT 如何及时生效？

**标准回答**：

这是一个经典的 JWT 无状态认证的固有矛盾：token 签发后内容不可变，角色变更无法即时反映。

**当前方案（token 短有效期）**：JWT 有效期配置为较短时间（建议 30 分钟 ~ 1 小时，当前项目配置在 application.yml 中）。角色变更后，最多等 1 小时旧 token 过期，用户下次登录拿到新 token 就生效了。

**升级方案 1 — Redis 黑名单**（适合需要即时生效的场景）：
1. 角色变更时，将该用户当前 JWT 的 `jti`（JWT ID，每次签发唯一）存入 Redis：`SET blacklist:{jti} 1 EX {剩余有效秒数}`
2. `JwtAuthenticationFilter.doFilterInternal()` 中增加一次 Redis 查询：`if (redisTemplate.hasKey("blacklist:" + jti)) → 拒绝`
3. 这样旧 token 立即失效，而 Redis 查询开销小（单次 O(1)）。

**升级方案 2 — 数据库 version 字段**（更轻量，适合不想维护 Redis 的场景）：
1. `t_user` 表加一个 `token_version` 字段（整数）
2. JWT payload 中带上 `token_version`
3. 角色变更时 `token_version++`
4. 过滤器校验时：拿 JWT 中的 version 和数据库当前 version 对比，不一致则拒绝

方案 2 的问题是每次请求都要查数据库（虽然没有 Redis 依赖），可以用缓存优化（查一次后缓存 5 分钟）。

**面试官追问**：「你更倾向哪个方案？」——方案 1（Redis 黑名单），因为 Redis 已经在架构中了（用来做 AI 缓存和热门排行），不引入额外依赖。`EXPIRE` 机制还能自动清理过期黑名单条目，不用手动维护。

---

### Q17. accessDeniedHandler 返回了 403 JSON 而不是 Spring Security 默认的重定向，为什么？

**标准回答**：

这是前后端分离架构的一个常见陷阱。Spring Security 默认的 `AccessDeniedHandler`（`AccessDeniedHandlerImpl`）会做两件事：
1. 如果用户未登录（Anonymous）→ 重定向到 `/login`（HTTP 302）
2. 如果用户已登录但权限不够 → 返回 403 错误页面（默认是 Spring Security 内置的 HTML 页面）

在前后端分离架构中，前端用 `fetch`/`axios` 发请求，它期待的是 JSON 响应。如果后端返回 302 重定向，`fetch` 会**自动跟随重定向**，最终拿到的可能是 `/login` 页面的 HTML。前端代码里的 `response.json()` 会抛 `SyntaxError: Unexpected token <`，因为拿到的是 HTML 不是 JSON。

问题的根源是：`fetch` 自动跟随重定向时，浏览器不会告诉前端「我经历了 302」，而是直接返回最终页面的响应（状态码 200 + HTML）。前端完全不知道发生了什么。

**解决方案**：

```java
// SecurityConfig 中注册自定义 handler
.exceptionHandling(ex -> ex
    .accessDeniedHandler((request, response, ex2) -> {
        response.setStatus(403);
        response.setContentType("application/json;charset=UTF-8");
        response.getWriter().write(
            JSONUtil.toJsonStr(Result.error(403, "权限不足，无法访问此资源")));
    })
)
```

返回 `{ "code": 403, "message": "权限不足..." }` JSON。前端可以统一拦截 `response.code === 403` 做提示：弹 toast「您没有权限执行此操作」或跳转到 403 页面。

类似地，`authenticationEntryPoint` 处理 401（未登录），返回 JSON 而非重定向。

**面试官追问**：「为什么不处理成 302？」——因为 302 对 fetch 不可控。参见上述自动跟随重定向的问题。唯一的例外是：如果前端用 `XMLHttpRequest` 且手动设置了 `xhr.responseType` 为 `document`，可以看到 302——但现代前端没人这么写。

---

## 三、RBAC 权限模型（深度）

### Q18. 三角色（PATIENT/DOCTOR/ADMIN）的权限边界是怎么划分的？

**标准回答**：

角色设计遵循「最小权限原则」，三个角色权限边界清晰：

| 能力 | PATIENT (0) | DOCTOR (1) | ADMIN (2) |
|------|:-----------:|:----------:|:---------:|
| 登录注册 | ✅ | ✅ | ✅ |
| AI 预问诊 | ✅ | ✅ | ✅ |
| 查看热门排行 | ✅ | ✅ | ✅ |
| 预约挂号（book） | ✅ | ❌ | ❌ |
| 查看自己的预约 | ✅ | ❌ | ❌ |
| 取消自己的预约 | ✅ | ❌ | ❌ |
| 确认/完成预约 | ❌ | ✅（仅自己的患者） | ❌ |
| 查看医生接诊的预约 | ❌ | ✅ | ❌ |
| 创建电子病历 | ❌ | ✅ | ❌ |
| 查看自己的病历 | ✅ | ✅（仅接诊的） | ❌ |
| 管理知识库 | ❌ | ❌ | ✅ |
| 管理科室/医生 | ❌ | ❌ | ✅ |
| 查看所有数据 | ❌ | ❌ | ✅ |

实现层面分两层：

**Controller 层（方法级权限）**：`@PreAuthorize("hasRole('PATIENT')")` 控制接口访问。例如 `AppointmentController.book()` 只有 PATIENT 能调。

**Service 层（数据范围）**：即使角色能调接口，能「看到什么数据」还要再过滤。例如 DOCTOR 调 `MedicalRecordController.pageList()`，Service 层会自动把查询限定为自己接诊的患者。

**面试官追问**：「DOCTOR 能不能给自己看病历？」——不能。医生的 role 是 DOCTOR，但作为「患者」的 user_id 不同。医生如果想以患者身份使用问诊功能，需要注册一个 PATIENT 角色的账号。

---

### Q19. 你如何在 Service 层实现「数据范围过滤」？（行级安全）

**标准回答**：

这是 RBAC 中比方法级注解更深的一层——「同一张表，不同角色能看到的数据行不同」。

核心实现在 `MedicalRecordServiceImpl.pageList()` 中：

```java
@Override
public PageResult<MedicalRecord> pageList(Long patientId, Long doctorId,
        Long currentUserId, Integer currentRole, long pageNum, long pageSize) {
    
    RoleType role = RoleType.fromCode(currentRole);
    Long queryPatientId = patientId;
    Long queryDoctorId = doctorId;

    if (role == RoleType.PATIENT) {
        // 患者只能查自己的病历 → 强制覆盖 patientId
        queryPatientId = currentUserId;
    } else if (role == RoleType.DOCTOR) {
        // 医生只能查自己接诊的病历 → 查出医生 ID 做过滤
        Doctor doctor = doctorMapper.selectByUserId(currentUserId);
        if (doctor != null) {
            queryDoctorId = doctor.getId();
        }
    }
    // ADMIN: 不加过滤，queryPatientId 和 queryDoctorId 保持前端传入值

    Page<MedicalRecord> page = new Page<>(pageNum, pageSize);
    var result = medicalRecordMapper.selectRecordPage(page, queryPatientId, queryDoctorId);
    return PageResult.of(result.getRecords(), result.getTotal(), pageNum, pageSize);
}
```

关键设计点：
1. **参数覆盖，而非仅校验**。PATIENT 角色时，`queryPatientId` 被**强制覆盖**为 `currentUserId`——用户可能在前端修改请求参数试图查别人的病历，但后端不信任前端传来的 `patientId`，直接覆盖。
2. **两层索引**。`selectRecordPage` 在 SQL 中按 `patient_id` 和 `doctor_id` 做 WHERE 过滤，索引命中效率高。
3. **ADMIN 无过滤**。不加 WHERE 条件，可以看所有人的病历——这是管理员的数据特权。

类似的，`MedicalRecordServiceImpl.detail()` 中查单条病历时也做角色校验——不是 ADMIN 又查的不是自己的/接诊的，直接抛 403。

**面试官追问**：「这种数据范围过滤能否用 MyBatis 拦截器 / SQL 注入实现？」——可以。MyBatis Interceptor 可以在 SQL 执行前自动拼接 WHERE 条件，但我们这里选择在 Service 层做，原因是：(1) 不同角色的过滤逻辑差别很大（医生要查 doctor_id，患者要查 patient_id），拦截器里要维护角色-列映射表，复杂度更高；(2) Service 层做过滤，代码意图更清晰，单测更好写。

---

### Q20. 如果 PATIENT 角色用户直接调用 POST 请求创建病历，你的系统怎么防御？

**标准回答**：

两层防线：

**防线 1 — Controller 层（Spring Security 注解拦截）**：
```java
@PostMapping
@PreAuthorize("hasRole('DOCTOR')")
public Result<MedicalRecord> create(@RequestBody MedicalRecordDTO dto) { ... }
```
如果 PATIENT 用户的 request 到达这个方法之前，Spring Security 的 `MethodSecurityInterceptor` 会检查 SecurityContext 中的 `Authentication.getAuthorities()`——发现没有 `ROLE_DOCTOR`，直接抛 `AccessDeniedException`，被 GlobalExceptionHandler 转为 `Result.error(403, "权限不足")`。

**防线 2 — Service 层（业务校验，纵深防御）**：
```java
Doctor doctor = doctorMapper.selectByUserId(doctorUserId);
if (doctor == null) {
    throw new BizException(403, "当前用户无医生档案，无法创建病历");
}
```
万一 `@PreAuthorize` 因配置错误没生效（比如 `@EnableMethodSecurity` 被误删），Service 层的二次校验也能拦住——查不到医生档案就不创建病历。

这是纵深防御（Defense in Depth）的思想：不依赖单一防线，每一层都能拦截。

**面试官追问**：「Controller 的 @PreAuthorize 和 Service 的二校有必要同时存在吗？」——有。Controller 注解是框架级防御，Service 校验是业务数据级防御。框架级防御可能因配置失误而失效（人误操作），业务级防御在代码逻辑中，不受配置影响。

---

### Q21. RoleType 枚举里 authority 为什么是 `ROLE_PATIENT` 而不是 `PATIENT`？

**标准回答**：

这是 Spring Security 的命名约定。`hasRole("PATIENT")` 在框架内部会被转换为 `hasAuthority("ROLE_PATIENT")`。这个转换发生在 `SecurityExpressionRoot.hasRole()` 方法中：

```java
// Spring Security 内部源码（简化）
public final boolean hasRole(String role) {
    return hasAuthority("ROLE_" + role);
}
```

所以如果用 `hasRole("PATIENT")` 做注解判断，SecurityContext 中的 `GrantedAuthority` 必须是 `ROLE_PATIENT`，不能是 `PATIENT`。

本项目中 `RoleType` 的 `authorityOf()` 方法：
```java
public static String authorityOf(Integer code) {
    RoleType role = fromCode(code);
    return "ROLE_" + role.name();  // ROLE_PATIENT / ROLE_DOCTOR / ROLE_ADMIN
}
```

如果这里不加 `ROLE_` 前缀，`@PreAuthorize("hasRole('PATIENT')")` 永远返回 403——因为框架去 SecurityContext 中找 `ROLE_PATIENT`，但实际存的是 `PATIENT`，匹配不上。

**面试官追问**：「如果我想用 `@PreAuthorize("hasAuthority('PATIENT')")` 会怎样？」——那就直接匹配 `PATIENT` 字符串，不需要 `ROLE_` 前缀。`hasAuthority` 做精确匹配，`hasRole` 自动加前缀。两种写法都行，项目中统一用 `hasRole` 是为了语义清晰——一眼看出这是「角色判断」而非「任意权限判断」。

---

### Q22. 预约挂号中，取消预约的权限逻辑是怎么设计的？

**标准回答**：

取消预约的权限逻辑不依赖 `@PreAuthorize`（方法级角色注解），而是在 Service 层实现更精细的「资源归属」判断：

```java
@Override
public Appointment cancel(Long id, Long operatorUserId, String reason) {
    Appointment appointment = appointmentMapper.selectAppointmentDetail(id);
    if (appointment == null) {
        throw new BizException(404, "预约记录不存在");
    }
    // 状态保护
    if (appointment.getStatus() == 3) {
        throw new BizException(400, "该预约已取消");
    }
    if (appointment.getStatus() == 2) {
        throw new BizException(400, "已完成的预约不可取消");
    }
    
    // 资源归属判断：患者本人 或 对应医生 可取消
    boolean isPatient = appointment.getPatientId().equals(operatorUserId);
    Doctor doctor = doctorMapper.selectByUserId(operatorUserId);
    boolean isDoctor = doctor != null && doctor.getId().equals(appointment.getDoctorId());
    
    if (!isPatient && !isDoctor) {
        throw new BizException(403, "无权取消此预约");
    }
    
    appointmentMapper.updateStatus(id, 3, reason);
    return appointmentMapper.selectAppointmentDetail(id);
}
```

关键设计点：
1. **非对称取消权**：预约双方都可取消——患者取消（不想看了），医生取消（临时有事停诊）。这是业务场景决定的，不是「谁的资源只能谁操作」的简单规则。
2. **状态机保护**：取消前先检查当前状态，已完成(2)的不允许取消。这是和前面 cancel 调用方的 Controller `@PreAuthorize` 不同的安全维度——一个是「你有没有权限」，一个是「当前状态下允不允许」。
3. **双重身份判断**：医生用户需要查 `doctorMapper.selectByUserId(userId)` 确认是医生、且是该预约对应的医生。不能只判断 role=1——因为 role 存在 JWT 中可能过时（被降级），数据库查 `doctors` 表是实时校验。

**面试官追问**：「为什么不直接用 `appointment.getDoctorId().equals(operatorUserId)` 判断医生？」——因为 `appointment.doctor_id` 是 `doctors` 表的主键（医生档案 ID），而 `operatorUserId` 是 `t_user` 表的主键（用户 ID）。两个 ID 来自不同表，不能直接比。用户的 role=1 不代表他的 user_id 就是某个医生的 id。需要通过 `doctorMapper.selectByUserId(userId)` 查出医生档案，再用档案的 `doctor_id` 去比较。

---

## 四、多轮对话状态管理（核心亮点，务必吃透）

### Q23. 多轮对话是如何实现「上下文记忆」的？说出完整链路。

**标准回答**：

完整链路分为 7 步，跨前端 → Java → Python 三个层级：

**Step 1 — 前端创建/恢复会话**：
```
首次问诊 → Java 返回 sessionKey (UUID) → 前端存 localStorage
下次问诊 → 从 localStorage 读 sessionKey → 随 POST body 发给后端
```

**Step 2 — Java 加载会话上下文**：
`ConsultationServiceImpl.preDiagnosisMultiTurn()` 中：
- 用 `sessionService.findByKey(sessionKey)` 查 `DiagnosisSession` 表 → 拿到 session 对象
- 用 `chatHistoryService.buildHistoryText(sessionId, 10)` → 取最近 10 条消息拼接为文本

**Step 3 — 滑动窗口取历史消息**：
`ChatHistoryServiceImpl.listRecentBySession()`：
- 先 `SELECT COUNT(*)` 得该 session 总消息数
- 算 offset = max(0, total - 10)
- `LIMIT offset, 10` 取最后 10 条 → 按时间正序返回

**Step 4 — 拼接对话文本**：
`buildHistoryText()` 将 ChatHistory 列表渲染为：
```
【对话历史】
[第1轮 - 用户] 头晕头痛三天了
[第1轮 - AI] 请问您头痛的具体部位在哪？
[第2轮 - 用户] 额头两侧，胀痛
[第2轮 - AI] 了解了，请问有测量过血压吗？
...
```

**Step 5 — 注入 prompt 发给 Python AI**：
Java 将 `conversation_history` 字符串 + 可选 `conversation_summary` 通过 REST 发给 Python 的 `/api/ai/diagnosis`

**Step 6 — Python 判断多轮模式**：
```python
def _is_multi_turn(req):
    return bool(req.conversation_history and req.conversation_history.strip())
```
选择 `MULTI_TURN_SYSTEM_PROMPT` + `MULTI_TURN_HUMAN_TEMPLATE`，将 `{history}` / `{summary}` 变量注入

**Step 7 — AI 理解上下文生成回答**：
LLM 收到的 prompt 包含完整的最近对话历史，能理解「头晕」和「已经三天了」之间的关联，做出连贯的分析追问。

**面试官追问**：「如果 sessionKey 在前端丢了怎么办？」——重新开一个新会话。旧的聊天历史仍然存在数据库（通过 userId 可以恢复），只是前端 UI 上不会自动显示。这是当前设计的局限性。

---

### Q24. 为什么用两张表（DiagnosisSession + ChatHistory）而不是一张表？

**标准回答**：

这是经典的「会话元信息」与「会话消息」分离设计，遵循数据库规范化中的职责分离原则。

**如果揉成一张表**（在 ChatHistory 表里加 status、summary、messageCount 等字段），问题：
1. **数据冗余**：同一 session 的每条消息都重复存一份 summary 和 status，浪费存储
2. **更新异常**：想更新 session 的 summary，需要更新该 session 下的所有 ChatHistory 行（100 条消息改 100 行）
3. **查询耦合**：想要「列出我的所有会话」→ 需要 `SELECT DISTINCT session_id, summary FROM chat_history WHERE user_id=?`，语义不清

**分开两张表的优势**：
- `DiagnosisSession` 管「宏观」：谁在什么时间问了一次诊，状态、消息计数、摘要
- `ChatHistory` 管「微观」：每一轮用户说了什么、AI 回了什么、精确到毫秒的时间戳

这种设计在实际工程中非常常见——类似于 Git 的 `commit`（相当于 session）和 `diff`（相当于 history）：commit 有作者/时间/message，diff 有每一行的增删。

**面试官追问**：「两张表的关联是外键约束吗？为什么没用？」——`chat_history.session_id` 逻辑上外键关联 `diagnosis_session.id`，但 MySQL 中没有声明 `FOREIGN KEY` 约束。原因是：(1) MyBatis-Plus 的分页查询在关外键检查时性能更好；(2) 消息可以独立归档/清理，不受外键约束限制。关联由业务代码保证。

---

### Q25. 滑动窗口的实现逻辑是什么？为什么取「最近 10 条」？

**标准回答**：

滑动窗口的核心逻辑在 `ChatHistoryServiceImpl.listRecentBySession()`：

```java
public List<ChatHistory> listRecentBySession(Long sessionId, int limit) {
    long total = chatHistoryMapper.selectCount(
            Wrappers.<ChatHistory>lambdaQuery()
                    .eq(ChatHistory::getSessionId, sessionId));
    long offset = Math.max(0, total - limit);
    return chatHistoryMapper.selectList(
            Wrappers.<ChatHistory>lambdaQuery()
                    .eq(ChatHistory::getSessionId, sessionId)
                    .orderByAsc(ChatHistory::getCreateTime)
                    .last("LIMIT " + offset + "," + limit));
}
```

两步操作：
1. `SELECT COUNT(*) WHERE session_id = ?` → 得总消息数 total
2. `offset = max(0, total - 10)` → `LIMIT offset, 10` → 取最后 10 条

为什么取 10 条？核心考量是 **DeepSeek 模型的上下文窗口限制**：

- DeepSeek 的上下文窗口约 8K-32K tokens
- 一条问诊消息平均约 150-300 tokens（中文）
- 10 条消息 ≈ 1500-3000 tokens，加上系统 prompt（约 500 tokens）+ 知识库上下文（约 800 tokens）+ AI 输出预留（约 1000 tokens）≈ 总共约 5000 tokens
- 在 8K 窗口内留有约 3000 tokens 的余量，不会溢出

更深层的思考：**10 条 = 5 轮对话**（5 条用户消息 + 5 条 AI 回复）。对于问诊场景，5 轮通常足够覆盖「症状描述 → AI 追问症状细节 → 用户补充 → AI 追问病史 → 用户回答 → AI 诊断」的完整链路。

**面试官追问**：「如果 10 条不够，怎么动态调整窗口大小？」——可以根据估算的 token 数动态截断。先拿最近 20 条→估算 token（粗略按 1 字符 ≈ 0.4 token）→ 如果超出预算（如 4000 tokens），从最早的消息开始逐条剔除直到符合预算。

---

### Q26. `.last("LIMIT offset, limit")` 这个拼接有什么风险？更安全的写法是什么？

**标准回答**：

当前代码：

```java
.last("LIMIT " + offset + "," + limit)
```

这里的风险是：**SQL 拼接绕过了 MyBatis-Plus 的参数绑定机制**。虽然 `offset` 和 `limit` 都是 `long` 类型（由 Java 代码计算得出，不是用户输入），在当前实现中没有 SQL 注入风险。但这是一个不规范的写法——如果是字符串拼接用户输入，就会有注入风险。

**更安全的写法**：不用 `.last()` 拼接，改用 MyBatis-Plus 的 `Page` 分页：

```java
// 方案 A：用 MyBatis-Plus 内置分页
Page<ChatHistory> page = new Page<>(offset / limit + 1, limit);
chatHistoryMapper.selectPage(page,
    Wrappers.<ChatHistory>lambdaQuery()
        .eq(ChatHistory::getSessionId, sessionId)
        .orderByAsc(ChatHistory::getCreateTime));
return page.getRecords();
```

或者写 Mapper XML 用 `#{}` 参数绑定：

```xml
<select id="listRecentBySession" resultType="...">
    SELECT * FROM chat_history WHERE session_id = #{sessionId}
    ORDER BY create_time ASC
    LIMIT #{offset}, #{limit}
</select>
```

`#{}` 会被 MyBatis 编译为 `PreparedStatement` 的参数占位符 `?`，完全杜绝 SQL 注入。

**面试官追问**：「你为什么不改成 Page 分页？」——因为 Page 分页在 MyBatis-Plus 中默认做了 count 查询，而我们这里先手动 count 了一次，改 Page 会多一次 count。直接用 `.last()` 是性能优先的 trade-off（少一次 count SQL）。但面试中主动说出「这个写法不够规范，应该用 Page 或 Mapper XML」展示安全意识，是加分项。

---

### Q27. 当对话历史超过 20 条时，怎么处理 token 超长问题？

**标准回答**：

本系统用了 **滑动窗口 + 摘要压缩** 双层策略：

**第一层：滑动窗口**（已回答，取最近 10 条原始对话）

**第二层：摘要触发**（超过 20 条时激活）：

```java
if (session.getMessageCount() != null && session.getMessageCount() > SUMMARY_THRESHOLD) {
    sessionSummary = session.getSummary();
}
```

当消息总数超过 `SUMMARY_THRESHOLD(20)` 时，从 `diagnosis_session.summary` 字段读取 AI 生成的会话摘要，和滑动窗口内的最近 10 条原始消息一起注入 prompt：

```
【会话摘要（压缩态历史）】
患者自述头痛三天，位于额头两侧，胀痛，血压偏高...

【最近对话历史】  ← 滑动窗口
[第15轮 - 用户] 昨天测了血压150/95
[第15轮 - AI] 血压偏高，建议...
```

这样做的好处是：原始对话的**最近上下文**保留（让 AI 理解当前追问），较远的对话用**摘要压缩**（减少 token 消耗但保留关键信息）。

**面试官追问**：「摘要谁来生成？当前代码中实现了摘要生成吗？」——当前代码中 `summary` 是预留字段，摘要生成逻辑尚未实现。但在设计上，摘要应该由 Python AI 服务异步生成：当消息数达到阈值时，Java 把历史消息发给 Python → Python 生成摘要 → Java 写回 `diagnosis_session.summary`。可以用 `CompletableFuture.runAsync` 异步触发，不阻塞当前请求。

---

### Q28. 如果 sessionId 在前端被篡改（恶意用户拿到别人的 sessionKey），会发生什么？怎么修？

**标准回答**：

这是一个真实的安全漏洞。当前代码在 `preDiagnosisMultiTurn()` 中：

```java
DiagnosisSession session = sessionService.findByKey(request.getSessionId());
if (session == null) {
    session = sessionService.getOrCreate(null, userId);
}
```

只校验 sessionKey 是否存在，**不校验归属**（session 是否属于当前用户）。攻击路径：
1. 攻击者合法登录后拿到自己的 sessionKey：`abc-123`
2. 通过某种方式获取到受害者的 sessionKey：`xyz-789`
3. 攻击者调 `GET /api/medical/session/xyz-789/history` → 看到受害者的问诊历史
4. 攻击者用 `POST /api/medical/consultation` 带 `sessionId: xyz-789` → 在别人的会话中插入消息

**修复方案**：

`DiagnosisSession` 创建时绑定 `userId`（当前已经有这个字段），查询时加 userId 校验：

```java
// sessionService.findByKey 改为：
public DiagnosisSession findByKeyAndUser(String sessionKey, Long userId) {
    return sessionMapper.selectOne(
        Wrappers.<DiagnosisSession>lambdaQuery()
            .eq(DiagnosisSession::getSessionKey, sessionKey)
            .eq(DiagnosisSession::getUserId, userId));  // 加 userId 过滤
}
```

`ChatHistoryController` 中查询历史时也加 userId 校验：

```java
// GET /api/medical/session/{sessionKey}/history
DiagnosisSession session = sessionService.findByKeyAndUser(sessionKey, userId);
if (session == null) throw new BizException(403, "无权访问此会话");
```

**面试官追问**：「sessionKey 是 UUID v4，暴力猜中概率极低，这个漏洞真的需要修吗？」——虽然 UUID 暴力猜中概率极低（2^122 的搜索空间），但 sessionKey 可能在以下场景泄露：(1) URL 参数中传递；(2) 浏览器控制台日志输出；(3) 服务端日志记录；(4) 被截图分享。所以安全最佳实践是 **不信任前端传来的任何 ID**，都做归属校验。

---

### Q29. 前端刷新页面后如何恢复多轮对话？sessionKey 怎么持久化？

**标准回答**：

当前前端的实现：

```typescript
// 创建新会话后
localStorage.setItem('consultation_session_key', sessionKey);

// 每次发问诊请求时
const sessionId = localStorage.getItem('consultation_session_key');
```

但存在一个已知局限：**页面刷新后，聊天 UI 不会自动恢复历史消息**。sessionKey 持久化仅在 localStorage 中，但 `messages` 数组（React state）会丢失。当前用户刷新后看到的是空白对话框，然后发新消息时才通过 sessionKey 恢复上下文。

**完整恢复方案应该怎么做**：

1. 页面 `useEffect` 初始化时，如果 localStorage 有 sessionKey → 调 `GET /api/medical/session/{sessionKey}/history` 加载历史消息
2. 把加载回来的 `ChatHistory[]` 转为前端 messages 数组格式
3. 渲染聊天气泡

但由于 `GET /api/medical/session/{sessionKey}/history` 接口**存在归属校验缺失**（Q28），当前的恢复链路不完整。

**面试官追问**：「localStorage 的 sessionKey 什么时候清理？」——用户点击「新问诊」时清理 `localStorage.removeItem()`。清理想的是避免旧对话历史干扰新问诊。

---

## 五、AI 集成：LangChain + RAG + CoT + SSE（最高频）

### Q30. 为什么要用 LangChain？直接用 HTTP 调 DeepSeek API 不就行了吗？

**标准回答**：

直接调 DeepSeek API（`requests.post("https://api.deepseek.com/v1/chat/completions")`）确实能实现基本的问答。但 LangChain 提供的不是「能不能调」的问题，而是「工程化」的问题：

**1. LLM 抽象层（Provider 无关）**：
```python
from app.core.llm import get_llm
llm = get_llm()  # 内部根据配置选择 DeepSeek / OpenAI / 通义千问
```
将来想换模型，只改 `.env` 中的 `LLM_PROVIDER` 和 API_KEY，代码一行不动。直接调 API 的话，每个 provider 的请求格式、认证方式、返回结构都不同，切换成本大。

**2. Prompt 模板系统**：
```python
prompt = ChatPromptTemplate.from_messages([
    ("system", MULTI_TURN_SYSTEM_PROMPT),
    ("human", MULTI_TURN_HUMAN_TEMPLATE),
])
messages = prompt.format_messages(symptom=..., context=..., history=...)
```
变量注入用模板变量 `{symptom}`，不会出「忘记转义」的问题。手动拼字符串容易出现 prompt 中包含了 `{}` 导致的格式化错误。

**3. 向量存储统一接口**：
```python
from app.core.vectorstore import get_vectorstore
docs = vectorstore.similarity_search(symptom, k=top_k)
```
内部可以是 ChromaDB，换成 FAISS 或 Milvus 只需要改 `get_vectorstore()` 实现，调用方零修改。

**4. 流式输出标准化**：
```python
for chunk in llm.stream(messages):
    content = getattr(chunk, "content", None)
    if content:
        yield content
```
`llm.stream()` 返回标准迭代器，不关心底层是 SSE、WebSocket 还是 gRPC stream。

**面试官追问**：「那为什么不直接用 Dify / LangFlow 这些低代码平台？」——低代码平台适合非技术人员做原型，但自定义逻辑（如多层降级、JSON 容错解析、摘要生成）在平台上很难实现。纯代码方案灵活性更高，适合往生产环境演进。

---

### Q31. RAG（Retrieval-Augmented Generation）在你的系统中是怎么工作的？具体流程是什么样的？

**标准回答**：

RAG 的核心思想是「检索 + 增强 + 生成」——不让 LLM 凭空编造，而是先检索相关医学知识，然后用检索到的知识作为 prompt 上下文指导 LLM 生成回答。

本系统的 RAG 流程：

**Step 1 — 症状向量化检索**（ChromaDB）：
```python
def _retrieve_context(symptom: str, top_k: int) -> str:
    vectorstore = get_vectorstore()
    docs = vectorstore.similarity_search(symptom, k=top_k)
    # docs 是按语义相似度排序的医学知识片段
```

**Step 2 — 组装为 prompt 上下文**：
将检索到的文档拼接为：
```
【知识库相关医学知识（参考依据）】
[1] 高血压常见症状包括头痛、头晕、心悸...诊断标准为...
[2] 紧张性头痛多表现为双侧额部或枕部持续性钝痛...
[3] 偏头痛特征为单侧搏动性头痛...
```

**Step 3 — 注入 prompt 发送给 LLM**：
```python
messages = prompt.format_messages(
    symptom=req.symptom_description,
    context=context,  # ← 检索到的知识片段注入
    ...
)
llm.invoke(messages)
```

**Step 4 — LLM 基于知识库回答**：
LLM 看到的 prompt 包含「知识库提供了高血压、紧张性头痛、偏头痛三个方向」的信息，生成回答时会引用这些知识，回答有据可依，不会凭空编造不存在的疾病名。

**容错设计**（这是加分点）：向量检索可能失败（ChromaDB 挂了、网络不通），不能因为检索失败就让整个问诊功能不可用：
```python
except Exception as exc:
    logger.warning("向量检索失败，降级为无上下文模式: %s", exc)
    return "知识库检索暂不可用，请结合自身医学常识作答。"
```

**面试官追问**：「ChromaDB 的数据是怎么来的？」——这正是 production-ready 的关键。当前是部署时从本地文件导入。生产环境需要：爬取医学百科/临床指南 → 文档切片（Chunking，300-500 字符/块 + 50 字符 overlap）→ embedding（bge-large-zh-v1.5，1024 维）→ 存入 ChromaDB。Chunk 不能太大（超 embedding 模型的 512 token 截断限制）也不能太小（丢掉上下文）。

---

### Q32. 请解释思维链（Chain-of-Thought）在你的 prompt 设计中怎么体现的？每一步做什么？

**标准回答**：

CoT 的核心是「让 LLM 先推理，再给答案」。本项目在 `SYSTEM_PROMPT` 中定义了严格的五步推理链：

```
【推理步骤（思维链 CoT）】
步骤1：提取患者主诉与关键症状，识别是否存在急危重症信号。
步骤2：关联知识库中与症状相关的医学知识条目。
步骤3：结合患者个体情况（年龄、性别、既往病史、过敏史），列出 2~4 个疑似疾病方向。
步骤4：评估风险等级——low（居家观察）/ mid（建议就诊）/ high（立即就医）。
步骤5：给出建议就诊科室、居家护理建议，以及必须立即就医的警示信号。
```

每一步为什么要按这个顺序：

**步骤1（急危重症筛查）必须放第一步**：因为这是安全底线。如果患者描述「剧烈胸痛、呼吸困难」，必须在推理早期就识别出来，后续步骤的优先级都低于急救提示。不能先分析可能性再判断风险——那样可能在分析过程中错过了危重信号。

**步骤2（知识库关联）在步骤3（疾病判断）之前**：因为 RAG 的目的就是让 LLM 基于外部知识做判断，而不是依赖它的训练数据。先召回相关知识，再列疑似疾病，避免 LLM 凭空编造。

**步骤3 → 4 的顺序**：先列出疾病方向（心脏病？胃食管反流？），再评估风险等级。因为风险等级取决于最可能的疾病方向——冠心病的风险是 high，胃食管反流的风险是 low。先列疾病再定级，逻辑自洽。

**步骤5 是落地输出**：用户真正需要的是「我该去看哪个科」「在家要注意什么」「什么情况下必须马上去急诊」。

**面试官追问**：「你怎么验证 CoT 真的有起作用？直接问症状不也能出结果吗？」——可以通过对比实验验证：同样的症状描述，用 CoT prompt vs 不用 CoT prompt，看输出结果的 (1) 疾病准确率；(2) 风险分级合理性；(3) 是否遗漏急危重症提示。CoT 通常在复杂推理场景（多症状交织）比直接问答强 20-30%。

---

### Q33. 单轮问诊和多轮问诊用了不同 prompt，为什么？能不能合一？

**标准回答**：

单轮和多轮 prompt 的核心区别在于 **对话模式的判断**：

**单轮 prompt**（`SYSTEM_PROMPT` + `HUMAN_TEMPLATE`）：
- 假设用户一次性描述了所有症状，直接走完整的 5 步 CoT 推理
- 要求输出**结构化 JSON**（`risk_level`/`possible_diseases`/`advice` 等字段）
- 配合 Few-shot 示例，规范输出格式

**多轮 prompt**（`MULTI_TURN_SYSTEM_PROMPT` + `MULTI_TURN_HUMAN_TEMPLATE`）：
- 增加了 `【多轮对话规则】`段——告诉 LLM：你现在处于对话中，用户在逐步补充信息
- 关键指令：**「如果用户本轮只是回答追问，则给出进一步分析或追问，无需输出 JSON」**
- 额外注入 `【会话摘要】`和 `【最近对话历史】`区域

为什么不能合一：如果多轮也用单轮 prompt，用户说「已经三天了」，LLM 会在没有足够信息的情况下强行输出诊断 JSON（幻觉）。如果单轮用多轮 prompt，用户一次性描述了完整症状，LLM 可能还在那里追问「请问有没有家族病史？」——交互效率低。

**面试官追问**：「多轮模式下 LLM 怎么知道该输出 JSON 还是不输出？」——在 `MULTI_TURN_SYSTEM_PROMPT` 中通过自然语言指令：「如果用户本轮明确要求诊断结论，或已有足够信息（3轮以上），则输出 JSON」。这是利用 LLM 的指令跟随能力做条件分支，比硬编码逻辑更灵活。

---

### Q34. SSE 流式输出和普通 HTTP 请求有什么区别？为什么要用 SSE？

**标准回答**：

| 维度 | 普通 HTTP（同步） | SSE 流式 |
|------|-----------------|---------|
| 响应模式 | 服务端完整生成后一次性返回 | 服务端边生成边推送 |
| 用户体验 | 输入症状后等 5-10 秒，然后一大段文字突然出现 | 看到 AI 一个字一个字「打出来」，感知等待时间短 |
| 数据方向 | 单向（请求→响应） | 单向（服务端→客户端） |
| 协议基础 | HTTP | HTTP（长连接 + `text/event-stream`） |
| 浏览器支持 | 所有 | 所有（`EventSource` API） |
| 断线重连 | 客户端手动重试 | 浏览器自动重连 + `Last-Event-ID` |

**为什么 SSE 适合 LLM token 流式输出**：

LLM 生成 token 是递增的——先出 `{"`，再出 `risk`，再出 `_level`...等 5-10 秒才能拿到完整 JSON。如果用同步 HTTP，用户看到空白界面 10 秒，然后跳出一大段 AI 诊断结果——体验很差，像「卡住然后突然蹦出来」。

用 SSE 流式：`token → HTTP chunk → 前端逐字渲染`。用户看到「高血压...心血管内科...建议...」一个字一个字地出现，感知延迟接近 0。这和 ChatGPT 的打字体验一致。

**为什么不选 WebSocket**：WebSocket 是全双工的（双向实时通讯），适合聊天室、协作编辑、游戏同步。但 LLM 流式输出是标准的生产者-消费者模型：服务端生产 token，客户端消费 token。SSE 足够，WebSocket 是杀鸡用牛刀——额外的握手开销、心跳保活、Nginx 配置复杂度都不值得。

**面试官追问**：「SSE 的 `data:` 行格式是什么样的？」——`data: {"type":"token","content":"高血压"}\n\n`。双换行符 `\n\n` 是一条消息的结束标记。Python 端 `yield f"data: {json.dumps(msg)}\n\n"`。

---

### Q35. SSE 首帧协议 `{"type":"meta","session_id":"..."}` 的作用是什么？

**标准回答**：

首帧协议的目的是：**在 SSE 连接建立后的第一时间，向客户端确认会话身份**。

流程：
```
客户端建立 SSE 连接 → Python 收到请求 → 
Python 生成 session → yield {"type": "meta", "session_id": "uuid-xxx-..."} → 
后续 token → yield {"type": "token", "content": "高血压..."} → 
结束 → yield {"type":"complete"}
```

这个设计解决了几个问题：

**1. 前后端 session 对齐**：SSE 连接建立后，客户端需要知道这次的问诊对应哪个 session_id（用来做下一轮多轮对话）。如果不发首帧，客户端得等整个流结束才能拿到 session_id——但如果客户端想中断问诊并发下一轮追问，就没有 session_id 可用。

**2. 连接状态确认**：首帧到了 = 连接成功建立、服务端准备就绪。前端可以据此更新 UI 状态（「AI 正在分析您的症状...」）。

**3. 后续多轮对话**：前端拿到 `session_id` 后存到 `localStorage`，下一次 POST `/api/medical/consultation` 时带上 `{ "sessionId": "uuid-xxx-..." }`，实现多轮对话的 session 串联。

**面试官追问**：「如果首帧丢失了怎么办？」——设计中要求 `meta` 帧必须是第一条 `yield`。如果因网络问题丢包，TCP 层会重传（SSE 基于 HTTP，底层 TCP）。但 `EventSource` API 层面没有确认机制，所以不能 100% 保证。生产环境建议加前端超时重连逻辑。

---

### Q36. AI 服务调用超时怎么办？你的降级策略是什么？三层降级分别做什么？

**标准回答**：

这是一个考察「系统可靠性设计」的问题。本系统在 Java 侧和 Python 侧都做了降级。

**Java 侧 — 超时与异步隔离**：

```java
CompletableFuture<AiDiagnosisResponse> future = CompletableFuture.supplyAsync(
        () -> callAiService(...), aiExecutor);

try {
    return future.get(aiTimeoutMs, TimeUnit.MILLISECONDS);
} catch (TimeoutException e) {
    log.warn("AI 服务调用超时（{}ms）", aiTimeoutMs);
    throw new BizException("AI 服务响应超时，请稍后重试");
}
```

设计要点：
- `CompletableFuture.supplyAsync` + 独立线程池 `aiExecutor` → AI 调用不占用 Tomcat 工作线程
- `future.get(timeout)` → 超时后抛出 `TimeoutException`，不无限阻塞
- 超时时间可配置（`health.ai.timeout-ms`），根据模型推理速度调整

**Python 侧 — 三层降级**：

```python
# 第一层：结构化链路（标准 CoT + PydanticParser）
try:
    return chain_builder.get_chain().invoke(variables)
except Exception:
    ...

# 第二层：原始输出 + 容错 JSON 解析
try:
    raw = llm.invoke(variables["symptom"] + context)
    payload = _extract_json(text)
    return DiagnosisResult.model_validate(payload)
except Exception:
    ...

# 第三层：兜底建议
if get_settings().enable_fallback:
    return _fallback_result(req)
```

**第一层**：标准链路——CoT prompt + Few-shot + PydanticParser 解析。如果 LLM 输出格式不符合 JSON Schema（比如多了一个字段或少了一个括号），解析失败。

**第二层**：降级链路——用更简单的 prompt 让 LLM 输出，然后用正则容错解析（去掉 markdown 包裹、截取 {} 范围）。这层解决了「输出格式偏差但内容可能正确」的问题。

**第三层**：兜底——返回预制的 `_fallback_result()`（风险等级 mid、建议就医）。保证用户一定得到回复，不会看到「500 Internal Server Error」。

**面试官追问**：「为什么第一层失败就降级，不先重试？」——LLM 调用成本高（每次 API call 都花钱 + 时间），而且如果第一层失败原因是 prompt 太长或 LLM 本身问题，重试大概率也失败。直接降级更高效。可以加一层智能重试——如果是网络 timeout 就重试 1-2 次，如果是格式解析失败就降级。

---

### Q37. `_extract_json()` 函数为什么要处理 ` ```json ` 和 ` ``` ` 包裹？这是 LLM 常见问题吗？

**标准回答**：

这是和大模型集成的核心 dirty work。LLM 的输出不稳定——同一个 prompt，有时输出纯 JSON，有时输出带 markdown 代码块包裹的 JSON：

```
可能输出 A:
{
  "risk_level": "mid",
  ...
}

可能输出 B:
```json
{
  "risk_level": "mid",
  ...
}
```

可能输出 C:
```
根据您的描述，我给出以下分析：
{
  "risk_level": "mid",
  ...
}
```

容错解析的三步处理：

```python
def _extract_json(text: str) -> dict:
    # Step 1: 去掉首尾的 markdown 代码块标记
    cleaned = text.strip()
    cleaned = re.sub(r"^```(?:json)?\s*", "", cleaned)
    cleaned = re.sub(r"\s*```$", "", cleaned)
    
    # Step 2: 找到第一个 { 和最后一个 }，截取 JSON 范围
    start, end = cleaned.find("{"), cleaned.rfind("}")
    if start == -1 or end == -1 or end <= start:
        raise ValueError("输出中未找到合法 JSON 对象")
    
    # Step 3: 解析 JSON
    return json.loads(cleaned[start:end + 1])
```

这个「脏活」不做会有什么后果：5-10% 的请求会因为 JSON 解析失败而报错 500。用户看到「AI 服务异常」，但实际 AI 已经正确生成了内容——只是包了一层 markdown。在 AI 应用工程中，这种「格式不稳定但内容正确」的情况非常普遍。

**面试官追问**：「有没有更好的方案？比如 Structured Output / Function Calling？」——有。DeepSeek 支持 JSON Mode（请求中传 `response_format: {"type": "json_object"}`），能显著降低格式错误率。但即使开了 JSON Mode，LLM 偶尔还是会在 JSON 前后加额外文本。所以容错解析仍然是必要的防御层。最彻底的方案是用 LangChain 的 `with_structured_output()` 方法，底层用 Function Calling 约束输出结构——不过这在 DeepSeek 上的支持程度还需要验证。

---

### Q38. `ChatPromptTemplate.format_messages()` 返回的是 `messages` 列表，为什么不是直接 string？

**标准回答**：

LangChain 的 `BaseChatModel` 接口设计为接收消息列表（`List[BaseMessage]`）而非纯字符串。每条消息包含角色标签（system / human / AI）和内容：

```python
[
    SystemMessage(content="你是一位专业 AI 预问诊助手..."),
    HumanMessage(content="患者症状：头晕...\n知识库：[1] 高血压...")
]
```

**为什么这样设计**：

1. **角色区分**：LLM 底层需要区分「系统指令」、「用户输入」、「AI 历史回复」三种不同角色。ChatGPT / DeepSeek 的 Chat Completions API 就是接收 `messages` 数组（`[{role: "system"}, {role: "user"}, {role: "assistant"}]`）。如果不区分角色，LLM 分不清哪些是指令、哪些是对话历史。

2. **多轮对话支持**：消息列表可以包含多轮 user/assistant 交替消息，天然支持对话历史注入。如果拼成一个大字符串再发给 LLM，LLM 的 tokenizer 就无法区分消息边界。

3. **流式输出兼容**：`llm.stream(messages)` 在底层把消息列表转换为 API 请求，流式处理返回的 token chunks。

**面试官追问**：「你的代码里为什么又用 `system` / `human` 字符串而不是 `SystemMessage` / `HumanMessage` 类？」——这是 LangChain `ChatPromptTemplate.from_messages()` 的简写语法。传 `("system", "文本")` 等价于 `SystemMessage(content="文本")`。LangChain 在 `format_messages()` 时自动转换。用元组写法更简洁。

---

### Q39. 向量检索的 ChromaDB 数据是怎么来的？生产环境需要做什么？

**标准回答**：

ChromaDB 的数据来源是一条 Pipeline：

```
原始医学文档（医学百科、临床指南、药品说明书）
    ↓ 文档切片（Chunking）
分割为 300-500 字/块，50 字 overlap（防止关键信息被切断）
    ↓ 向量化（Embedding）
用 bge-large-zh-v1.5 模型将每块转为 1024 维向量
    ↓ 存入 ChromaDB
向量 + 原始文本 + 元数据 一起存储
    ↓ 检索
用户症状文本 → embedding → similarity_search → top-k 最相似块
```

**关键决策点**：

**1. Chunk 大小**：300-500 字是经验值。太小（100 字）→ 丢失上下文关联；太大（1000 字）→ 检索精度下降、超过 embedding 模型输入限制。50 字 overlap 是为了避免关键句子恰好落在两个 chunk 的边界被截断。

**2. Embedding 模型选择**：`bge-large-zh-v1.5` 在中文语义检索上效果最好（MTEB 中文榜单排前 3）。1024 维向量，单条存储约 4KB。

**3. ChromaDB vs Faiss vs Milvus**：
- ChromaDB：开发友好，Python 原生命令，单机部署，适合本项目当前规模
- Faiss：纯向量检索，高性能但不支持元数据过滤，需要自己搭服务
- Milvus：分布式向量数据库，适合百万级+向量规模

**面试官追问**：「知识库多久更新一次？更新时怎么保证服务不中断？」——Blue-Green 部署模式：维护两套 ChromaDB（A/B），更新时写 B，切换流量到 B，旧的 A 做回滚。或者用 ChromaDB 的 collection versioning。

---

## 六、数据库设计与 SQL

### Q40. users 表的 role 字段怎么设计的？为什么用 TINYINT 而不是 ENUM 或 VARCHAR？

**标准回答**：

```sql
`role` TINYINT DEFAULT 0 COMMENT '角色: 0患者 1医生 2管理员'
```

| 方案 | 存储 | 可扩展性 | 查询效率 | 缺点 |
|------|------|---------|---------|------|
| TINYINT | 1 byte | 好（0-255 种角色） | 高（整数比较） | 代码中要做好映射 |
| ENUM('PATIENT','DOCTOR','ADMIN') | 1-2 byte | 差（ALTER TABLE 才能加新值） | 中 | MySQL 特有，不标准 |
| VARCHAR(20) | 最多 21 bytes | 好 | 低（字符串比较） | 空间浪费 |

选 TINYINT 的核心原因：
1. **空间效率**：TINYINT 仅 1 字节，VARCHAR(10) 至少 11 字节（1 字节长度前缀 + 10 字节内容），100 万用户差异是 10MB vs 110MB
2. **索引效率**：整数 B+Tree 比较比字符串快
3. **与应用层解耦**：ENUM 值定义在 MySQL 中，改需要 ALTER TABLE；TINYINT 的映射在 Java `RoleType` 枚举中，改映射不需要迁移数据库

**Java 侧映射**：
```java
public enum RoleType {
    PATIENT(0, "ROLE_PATIENT"),
    DOCTOR(1, "ROLE_DOCTOR"),
    ADMIN(2, "ROLE_ADMIN");
    
    public static RoleType fromCode(Integer code) { ... }
    public static String authorityOf(Integer code) { ... }
}
```

**注意**：`t_doctor.user_id` 关联 `t_user.id`——不是所有 role=1 的用户都在 `doctors` 表中有记录。这是「角色」与「具体档案」的解耦——用户可以标注为 DOCTOR 角色，但要真正当医生用，还必须在 doctors 表中创建档案。

**面试官追问**：「如果将来要支持「一个用户多个角色」（如既是患者又是管理员），TINYINT 还能用吗？」——不能。多角色需要用位运算（BIT 字段 + `role & 1 = 1` 判断患者）或用关联表（`user_roles` 表 + JOIN）。当前设计假设每个用户单一角色。

---

### Q41. appointment 表的状态机是怎么设计的？为什么不用字符串状态？

**标准回答**：

```sql
`status` TINYINT DEFAULT 0 COMMENT '状态: 0待确认 1已确认 2已完成 3已取消'
```

状态转换图：
```
          cancel()
    ┌─────────── 3 (已取消)
    │         
    0 (待确认) ── confirm() ──→ 1 (已确认) ── complete() ──→ 2 (已完成)
    │                                    │
    └─────── cancel() ───────────────────┘
```

**状态转换的严格校验**（在 `AppointmentServiceImpl` 中）：

```java
// confirm: 只能从「待确认」确认
if (appointment.getStatus() != 0) {
    throw new BizException(400, "仅待确认状态可确认");
}

// complete: 只能从「已确认」完成
if (appointment.getStatus() != 1) {
    throw new BizException(400, "仅已确认状态可完成就诊");
}

// cancel: 不能取消已取消(3)或已完成(2)的
if (appointment.getStatus() == 2) {
    throw new BizException(400, "已完成的预约不可取消");
}
if (appointment.getStatus() == 3) {
    throw new BizException(400, "该预约已取消");
}
```

**为什么不直接用 `updateStatus` 而不是 `UPDATE SET status = ? WHERE status = oldStatus`（乐观锁）**：

当前设计在 Service 层先查状态再判断再更新，存在竞态（两次 confirm 请求并发）。生产环境建议改为：

```sql
UPDATE appointment SET status = 1 WHERE id = ? AND status = 0
```
如果 `affectedRows = 0` 说明被其他请求抢先了，返回失败。这是乐观锁思想——不需要额外 version 字段，直接用业务状态字段做并发控制。

**为什么不选字符串状态**：
- TINYINT：索引大小 1 byte + 比较用整数运算
- VARCHAR：索引大小 3-10 bytes + 比较用字符串运算
- 在百万级预约表上，索引存储差异 10MB vs 30-100MB，查询性能差异约 30%

**面试官追问**：「状态码 0/1/2/3 在代码里看起来不直观，怎么改善？」——可以定义常量类或枚举 `AppointmentStatus`。项目中已在 Service 层注释了状态含义。

---

### Q42. medical_record 表是怎么关联到 appointment 的？为什么允许 appointment_id 为 NULL？

**标准回答**：

```sql
`appointment_id` BIGINT DEFAULT NULL COMMENT '关联的预约ID，可为空'
```

允许 NULL 的设计原因：

**场景 1：预约-病历闭环**。患者在系统上预约 → 医生确认 → 就诊 → 医生创建病历，关联预约 ID。患者端可以看到「预约 → 就诊 → 病历」的完整链路。

**场景 2：直接就诊（非预约）**。患者直接去医院，医生接诊后想在系统中记录病历，此时没有预约记录。如果 appointment_id 是 NOT NULL，医生就得先「创建一个虚拟预约」再写病历——不合理。

所以 nullable 是业务灵活性的需要，允许两种就诊路径并存。

**面试官追问**：「一条预约可以对应多条病历吗？」——按当前设计可以（因为 appointment_id 在 medical_record 表，不是 unique 约束）。但实际上一个预约应该只有一份病历（复诊是另一次预约）。可以在应用层做唯一性校验：创建前先查 `SELECT COUNT(*) FROM medical_record WHERE appointment_id = ?`。

---

### Q43. 预约挂号的时段容量校验是怎么做的？有没有并发问题？

**标准回答**：

```java
// book() 方法中
String dateStr = dto.getAppointmentDate().format(DateTimeFormatter.ISO_DATE);
int booked = appointmentMapper.countByDoctorDateSlot(
    dto.getDoctorId(), dateStr, dto.getTimeSlot());

if (doctor.getMaxDailyAppointments() != null && booked >= doctor.getMaxDailyAppointments()) {
    throw new BizException(400, "该时段预约已满，请选择其他时段");
}
```

流程：`COUNT 已预约数 → 比较容量上限 → 如果未满则 INSERT`

**并发问题**：这是典型的 Check-Then-Act 竞态条件。两个患者同时调 book，COUNT 都返回 19（容量 20），两个请求都判断「未满」，然后都 INSERT，最终该时段有 21 个预约（超挂了）。

**修复方案（按推荐优先级）**：

**方案 1 — 数据库唯一约束 + INSERT IGNORE**（最轻量）：
给 `(doctor_id, appointment_date, time_slot, patient_id)` 加唯一索引。如果同一患者重复预约同一时段，INSERT 失败。

但这只防「同一个患者重复预约」，不防「不同患者的超挂号」。

**方案 2 — 数据库乐观锁**（推荐）：
```sql
-- 不用 COUNT + INSERT，改为：
-- 先 UPDATE doctor SET max_daily_appointments = max_daily_appointments - 1 
-- WHERE id = ? AND max_daily_appointments > 0
-- 如果 affectedRows = 0 → 名额已满
```

**方案 3 — Redis 分布式锁**：
```java
String lockKey = "lock:appointment:" + doctorId + ":" + dateStr + ":" + timeSlot;
Boolean locked = redisTemplate.opsForValue().setIfAbsent(lockKey, "1", 10, TimeUnit.SECONDS);
if (!locked) throw new BizException("预约拥挤，请稍后重试");
// ... book ...
redisTemplate.delete(lockKey);
```

方案 2 最适合当前项目——不引入额外组件，利用了数据库的行锁特性。

**面试官追问**：「你现在的代码里有并发保护吗？」——没有。这是已知的并发漏洞。面试中主动说出问题和修复方案，比假装没问题要好。

---

### Q44. chat_history 表为什么不直接用 consultation_record 代替？

**标准回答**：

这是两种不同粒度的记录：

- `consultation_record`：**宏观**。一次完整的问诊提交（用户描述症状 → AI 给出诊断结果）。记录的是「这次问诊的结果」——风险等级、疑似疾病、就诊建议。
- `chat_history`：**微观**。一次问诊中的每一轮对话。记录的是「用户说了什么、AI 回了什么」。

举例：一次问诊可能包含 5 轮对话（用户描述、AI 追问、用户补充、AI 再追问、AI 给诊断）。consultation_record 存 1 条记录，chat_history 存 10 条记录（5 个 user + 5 个 assistant）。

**用 chat_history 单独存的理由**：

1. **滑动窗口取历史消息**：需要按 session 取「最近 N 条 user + assistant 消息」，按时间正序排列。如果在 consultation_record 里存一个大 JSON 字段，取出和解析的成本高，且没法按时间索引。
2. **前端聊天 UI 回显**：前端需要按时间线渲染聊天气泡，chat_history 的 `create_time` 列天然支持排序。
3. **灵活扩展**：未来可能加新类型的消息（如 `role: "system"` 表示系统提醒、`role: "attachment"` 表示图片/检查报告），chat_history 加类型枚举即可，不用改 consultation_record 表结构。

**面试官追问**：「chat_history 存 blob/JSON 类型不好吗？」——JSON 类型方便存但难查询。如果运营要分析「哪种追问方式最有效」，需要 `SELECT role, content FROM chat_history WHERE ...`，JSON 列需要 `JSON_EXTRACT` 然后 LIKE，性能很差。关系型字段（role 列 + content 列）可以直接建索引。

---

### Q45. 为什么问诊记录（consultation_record）既有 symptom_description 又有 chief_complaint？

**标准回答**：

```sql
`symptom_description` TEXT COMMENT '症状描述全文',
`chief_complaint` VARCHAR(255) COMMENT '主诉摘要(前30字)'
```

这是「全量 + 摘要」存储策略，类似数据库中「原始数据 + 物化视图」的思路。

**使用场景**：

- `symptom_description`：用户输入的原始全文（如「最近一周头晕头痛，特别是额头两侧胀痛，一量血压就高，晚上失眠多梦，白天精神不好...」约 80 字）。用于：详情页展示完整描述、AI 推理时注入 prompt、导出诊断报告。
- `chief_complaint`：前 30 字摘要（如「最近一周头晕头痛，特别是额头两侧胀痛，一量血压就高...」）。用于：列表页展示（不用加载 TEXT 全文，单页 20 条记录如果每条都查 TEXT 列，I/O 开销大）、搜索结果预览。

**为什么不在 Service 层动态截断**：
- 动态截断每次 `SELECT symptom_description` + Java 端 `substring(0, 30)`，数据库仍需读取整条 TEXT
- MySQL 的 TEXT 列存在行外（overflow page），读取 TEXT 需要额外 I/O
- VARCHAR(255) 和行数据存储在一起，读取主键索引时就能拿到，不额外 I/O

**面试官追问**：「为什么不直接用 VARCHAR(500) 存全文？」——因为用户可能描述很长（200+ 字），VARCHAR 上限 65535 但影响行大小。TEXT 存在行外，不影响主键索引的紧凑性。

---

## 七、缓存与性能

### Q46. Redis 在你的系统中承担了哪些职责？每个职责的 Key 设计和数据结构是怎样的？

**标准回答**：

| 职责 | Key 模式 | 数据结构 | 操作 |
|------|---------|---------|------|
| AI 结果缓存 | `ai:diag:cache:{md5(症状)}` | String (JSON) | `SET` + `EXPIRE` |
| 热门问诊排行 | `ai:diag:hot` | Sorted Set | `ZINCRBY` → `ZREVRANGE` |
| （未来）JWT 黑名单 | `blacklist:{jti}` | String | `SET` + `EXPIRE`（同 JWT 过期时间）|
| （未来）分布式锁 | `lock:appt:{doctorId}:{date}:{slot}` | String | `SETNX` |

**AI 结果缓存**详细设计：
```java
String cacheKey = "ai:diag:cache:" + DigestUtil.md5Hex(normalizeSymptom(symptom));
// 命中：直接返回，不调 AI
// 未命中：调 AI → 结果转 JSON → SET cacheKey value EX {ttl}
```

MD5 + 症状归一化（trim + toLowerCase）做 key，保证了相同症状查相同缓存。但有个已知局限：未把患者个体信息（年龄/性别/病史）纳入 key——不同的人相同症状可能有不同的建议。

**热门排行**详细设计：
```java
redisTemplate.opsForZSet().incrementScore("ai:diag:hot", normalizedSymptom, 1);
// 查询 Top N
Set<TypedTuple<Object>> tuples = redisTemplate.opsForZSet()
    .reverseRangeWithScores("ai:diag:hot", 0, topN - 1);
```

ZSet 的 `ZINCRBY` 是原子操作，无竞态。`ZREVRANGE` 按分数降序取 Top N，复杂度 O(log N + M)。

**面试官追问**：「Redis 挂了怎么办？缓存穿透/击穿/雪崩怎么防？」——
- 穿透（大量查不存在的数据）：当前 AI 缓存不存在「不存的 key」问题，因为 key 由症状 MD5 生成。如果是用户 ID → 用户信息缓存，可以用 Bloom Filter 或缓存空值。
- 击穿（热点 key 过期瞬间大量请求）：加互斥锁——`SETNX lock:cache:{key}` 成功者去查 DB/调 AI，失败者 sleep 100ms 重试读缓存。
- 雪崩（大量 key 同时过期）：过期时间加随机偏移——`EXPIRE key ttl + random(0, 300)`。

---

### Q47. 预问诊结果缓存的 key 是怎么设计的？有碰撞风险吗？有缓存不一致问题吗？

**标准回答**：

```java
String cacheKey = "ai:diag:cache:" + DigestUtil.md5Hex(normalizeSymptom(symptom));
```

**碰撞风险**：MD5 有 2^128 种可能，生日悖论下要 2^64 个输入才达到 50% 碰撞概率。以问诊场景的实际症状输入数量（可能到百万级 = 2^20），碰撞概率可忽略不计。不是安全场景（安全场景用 SHA-256），MD5 碰撞风险为 0。

**缓存不一致问题**（同样的症状但不同的人）：

举例：
- 用户 A：男，30 岁，症状「头痛」，无病史 → AI 建议紧张性头痛
- 用户 B：女，60 岁，症状「头痛」，有高血压史 → AI 建议高血压

两人的症状归一化后相同，缓存 key 相同，但 AI 结果应该不同。当前 key 设计**没有包含个性化信息**，会导致缓存不准确。

**改进方案**：
```java
// 把年龄范围、性别也纳入 key
String personalizedKey = symptom + "|" + ageGroup + "|" + gender;
String cacheKey = "ai:diag:cache:" + DigestUtil.md5Hex(personalizedKey);
```

**面试官追问**：「那为什么不改成包含所有字段的 key？」——trade-off：key 粒度越细，缓存命中率越低。包含年龄+性别已经能覆盖大部分差异化场景，不需要降到「完全相同输入才命中」。

---

### Q48. 如果两个患者同时预约同一个医生的最后一个名额，怎么防止超挂号？

**标准回答**：

这是个经典的并发竞态问题，当前代码存在此漏洞。下面是具体场景和修复：

**场景模拟**：
```
时间线   请求A（患者X）                   请求B（患者Y）
T1      COUNT → 19（< 20）             COUNT → 19（< 20）
T2      INSERT → 第20个                 INSERT → 第21个 ❌ 超挂号！
```

**修复方案对比**：

| 方案 | 实现方式 | 优点 | 缺点 |
|------|---------|------|------|
| 乐观锁 | `UPDATE doctor SET version=version+1 WHERE id=? AND version=?` | 无锁竞争，性能好 | 冲突时需要重试 |
| 悲观锁 | `SELECT ... FOR UPDATE` | 简单，强一致 | 性能差（行锁等待） |
| Redis 分布式锁 | `SETNX lock:appt:...` + 业务执行 + DEL | 高效 | 组件依赖，锁超时风险 |
| MySQL UNIQUE | `INSERT ... ON DUPLICATE KEY` + 触发器减计数 | 数据库原生 | 不直观 |

**推荐方案：数据库乐观锁**（最适合当前项目规模）：

```sql
-- 不用 COUNT + INSERT，改为：
UPDATE doctor SET max_daily_appointments = max_daily_appointments - 1 
WHERE id = #{doctorId} AND max_daily_appointments > 0
```

`UPDATE` 在 InnoDB 中是一个原子操作——WHERE 条件 `max_daily_appointments > 0` 加上行锁，两个并发请求中只有一个能成功（affectedRows = 1），另一个返回 0 → 提示「预约已满」。

这个方案的好处：不依赖 Redis（当前项目 Redis 是缓存，不是强一致存储），不引入额外锁机制。

**面试官追问**：「乐观锁重试几次？」——通常 3 次，每次间隔随机 10-50ms。超过 3 次 → 返回「当前预约人数较多，请稍后重试」。这是因为预约挂号场景的并发冲突率通常 < 5%，重试成本低。

---

### Q49. AI 服务调用为什么要用 CompletableFuture + 独立线程池？这和直接用 RestTemplate 有什么区别？

**标准回答**：

```java
@Resource(name = "aiExecutor")
private Executor aiExecutor;

CompletableFuture<AiDiagnosisResponse> future = 
    CompletableFuture.supplyAsync(() -> callAiService(...), aiExecutor);
return future.get(aiTimeoutMs, TimeUnit.MILLISECONDS);
```

**核心问题**：Tomcat 默认线程池（约 200 个线程）是**所有 HTTP 请求共用**的。AI 调用是 I/O 密集型（等 LLM 返回 5-10 秒），如果同步调用发生在 Tomcat 线程上：

```
并发场景：100 个问诊请求同时来
→ 100 个 Tomcat 线程全部阻塞在 restTemplate.postForEntity() 等 AI 返回
→ 第 101 个普通请求（查科室列表）没线程可用 → 请求排队/超时
```

**用独立线程池的隔离效果**：

```
并发场景：100 个问诊请求
→ Tomcat 线程接收请求 → 提交任务到 aiExecutor → Tomcat 线程释放
→ aiExecutor 线程池（50 个专门线程）并发调 AI
→ Tomcat 200 线程可以继续处理普通请求
```

虽然当前 `future.get(timeout)` 还是阻塞了 Tomcat 线程（因为要等 AI 返回才能给前端响应），但至少隔离了线程池——AI 线程池满（50 个全在等 AI）不会影响普通 CRUD 的 Tomcat 线程。

**真正的异步方案**（进一步提升）：
```java
// 1. 先同步返回一个任务ID
return Result.ok(new { taskId = uuid })
// 2. 后台线程异步调 AI
// 3. 前端轮询 GET /task/{taskId}/status 或 WebSocket 推送结果
```
这样 Tomcat 线程完全不阻塞，但实现复杂度高很多。

**面试官追问**：「future.get(timeout) 还是阻塞的，为什么不直接用 `@Async` 注解？」——`@Async` 也依赖线程池（Spring 的 `taskExecutor`），功能等价。但 `CompletableFuture` 的 API 更灵活（可以链式 thenApply/thenCompose/exceptionally）。当前场景选了 CompletableFuture。

---

### Q50. 热门问诊排行榜为什么用 Redis ZSet 而不是 MySQL ORDER BY count DESC？

**标准回答**：

| 操作 | Redis ZSet | MySQL |
|------|-----------|-------|
| 写入（每问诊一次 +1） | `ZINCRBY key 1 member` — O(log N)，内存操作 | `UPDATE hot_questions SET count=count+1 WHERE symptom=?` — O(log N) 索引查，但有磁盘 I/O |
| 读取 Top 10 | `ZREVRANGE key 0 9 WITHSCORES` — O(log N + 10) | `SELECT symptom, count FROM hot_questions ORDER BY count DESC LIMIT 10` — O(N log N) 全表排序 |
| 并发写入 | 单线程原子操作，天然无竞态 | 需要事务或乐观锁防竞态 |
| 自动清理 | `EXPIRE` / 内存淘汰策略 | 需要定时任务 DELETE 旧数据 |

核心差异是**写入频率**：
- 每次 AI 问诊都触发 `ZINCRBY`（高频写入）
- Top 10 查询频率低（首页加载）

MySQL 做排行榜的问题：高并发写入（`UPDATE count=count+1`）会锁行 → 锁竞争 → 性能瓶颈。Redis ZSet 的 `ZINCRBY` 是 O(log N) 的纯内存操作，天然支持高并发。

**面试官追问**：「Redis 是内存数据库，热门排行数据丢了怎么办？」——可以接受。热门排行不是核心业务数据（不像预约记录丢了就出事故）。冷启动时热门排行为空，首页展示空列表或热门科室兜底，不影响核心功能。如果一定要持久化，可以用 Redis AOF `appendfsync everysec`。

---

## 八、前端关键设计

### Q51. 前端怎么管理多轮对话状态？为什么不用 Redux / Zustand？

**标准回答**：

当前 `ConsultationPage.tsx` 中，多轮对话状态完全用 React 内置 hooks：

```typescript
const [messages, setMessages] = useState<Message[]>([]);
const [sessionKey, setSessionKey] = useState<string | null>(null);
const [loading, setLoading] = useState(false);
const inputRef = useRef<HTMLTextAreaElement>(null);
```

**为什么不用 Redux**：
- 对话状态仅在 `ConsultationPage` 一个组件中使用，不需要跨页面共享
- Redux 对一个组件内部的状态管理是杀鸡用牛刀——需要定义 action types、reducers、dispatch，代码量翻倍
- 当前 3 个 state 变量 + 几个 handler 函数，useState + useCallback 完全够用

**什么时候会引入 Zustand**：
- 如果加了「多窗口聊天」（左边列表、右边聊天区）→ 两个组件共享 messages 和 sessionKey → 需要全局状态管理 → Zustand
- 如果加了「通知系统」（新消息到来时顶部栏显示 badge）→ 跨组件订阅 → Zustand

Zustand 比 Redux 更适合本项目：API 极简（`create` → `useStore`），不需要 Provider 包裹，TypeScript 类型推导完美。

**面试官追问**：「messages 数组会无限增长吗？有做上限吗？」——当前没有。生产环境应该在 `setMessages(prev => [...prev, msg])` 前判断 `prev.length > 200` 时做截断（保留最后 100 条消息），避免 React diff 性能下降和内存泄漏。

---

### Q52. 如果用户输入「头晕」，AI 返回诊断结果，用户追问「已经三天了」，前端传什么给后端？

**标准回答**：

这个问题的关键在于：AI 需要理解「已经三天了」指的是什么。答案在前端保持 sessionKey 不变。

**请求序列**：

第一轮：
```json
POST /api/medical/consultation
{
  "symptomDescription": "头晕",
  "sessionId": null   // 首次，无 session
}

响应：
{
  "sessionKey": "uuid-abc-123",
  "aiAdvice": "请问您头晕持续了多长时间？有加重趋势吗？"
}
```

第二轮（追问）：
```json
POST /api/medical/consultation
{
  "symptomDescription": "已经三天了",
  "sessionId": "uuid-abc-123"   // 同一个 sessionKey
}
```

后端处理：
1. `preDiagnosisMultiTurn()` 用 `sessionKey` 查 `DiagnosisSession` → 拿到 session
2. `buildHistoryText(sessionId, 10)` → 加载历史消息：
   ```
   [第1轮 - 用户] 头晕
   [第1轮 - AI] 请问您头晕持续了多长时间？
   ```
3. 新消息「已经三天了」→ 拼入 prompt
4. AI 收到的完整上下文：
   ```
   【最近对话历史】
   用户：头晕
   AI：请问您头晕持续了多长时间？
   用户：已经三天了
   ```
5. AI 理解：用户在回答「持续时间」的追问 → 三天 → 综合分析

**关键**：`symptomDescription` 只传「已经三天了」三个字，不需要前端把整个对话历史全传过去——上下文由后端从数据库加载。

**面试官追问**：「如果用户在两条对话之间关掉浏览器再打开，前端怎么知道上次的 sessionKey？」——通过 `localStorage.getItem('consultation_session_key')` 恢复。但聊天 UI 不会自动恢复历史消息（已知局限，Q29 已讨论）。

---

### Q53. 前端怎么对接 SSE 流式输出？当前代码支持吗？

**标准回答**：

当前前端用的是普通 `POST` 请求（`fetch` + `response.json()`），非流式。但 Python 端已经实现了 SSE 流式接口（`/api/ai/diagnosis/stream`），前端对接方案有两种：

**方案 1 — EventSource（简单，但只支持 GET）**：
```typescript
const eventSource = new EventSource('/api/ai/diagnosis/stream?sessionId=xxx&symptom=...');
eventSource.onmessage = (event) => {
  const data = JSON.parse(event.data);
  if (data.type === 'meta') {
    setSessionKey(data.session_id);
  } else if (data.type === 'token') {
    // 逐字追加到最后一个 AI 消息气泡
    appendToLastMessage(data.content);
  }
};
```
缺点：`EventSource` 不支持 POST（查询参数有长度限制），且不支持自定义请求头（不能带 Authorization）。

**方案 2 — fetch + ReadableStream（推荐）**：
```typescript
const response = await fetch('/api/ai/diagnosis/stream', {
  method: 'POST',
  headers: { 'Authorization': `Bearer ${token}`, 'Content-Type': 'application/json' },
  body: JSON.stringify({ symptomDescription, sessionId, ... }),
});

const reader = response.body.getReader();
const decoder = new TextDecoder();

while (true) {
  const { done, value } = await reader.read();
  if (done) break;
  const text = decoder.decode(value);
  // 解析 SSE 格式: data: {...}\n\n
  const lines = text.split('\n');
  for (const line of lines) {
    if (line.startsWith('data: ')) {
      const data = JSON.parse(line.slice(6));
      // 按 type 分发
    }
  }
}
```

**面试官追问**：「为什么要 POST 而不是 GET？」——POST 可以传复杂的 JSON body（包括 symptomDescription 可能几百字、medical_history 等），GET 查询参数有 URL 长度限制（IE 2083 字符，Chrome 约 8000），不适合传长文本。

---

## 九、部署与 DevOps

### Q54. Docker Compose 中四个服务的启动顺序是怎么控制的？healthcheck 具体做了什么？

**标准回答**：

```yaml
backend:
  depends_on:
    mysql:
      condition: service_healthy   # 不只是容器启动了，还要服务就绪
    redis:
      condition: service_healthy
    ai-service:
      condition: service_started
```

**MySQL healthcheck**：
```yaml
healthcheck:
  test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-p${MYSQL_ROOT_PASSWORD:-root123}"]
  interval: 10s       # 每 10 秒检查一次
  timeout: 5s         # 单次检查最多等 5 秒
  retries: 10         # 连续失败 10 次才标记 unhealthy
  start_period: 30s   # 启动后前 30 秒，失败不计入 retries
```

`start_period: 30s` 是关键——MySQL 8.0 首次启动时，会做数据目录初始化 + InnoDB redo log 初始化 + 临时表空间创建，可能需要 20-60 秒。这期间的 healthcheck 失败是正常的，不应该被计入 retries。

**Redis healthcheck**：
```yaml
healthcheck:
  test: ["CMD", "redis-cli", "ping"]  # PING → PONG 表示就绪
  interval: 10s
  retries: 5
```
Redis 启动极快（1-2 秒），不需要 `start_period`。

**ai-service 为什么只有 service_started**：`ai-service` 的 Dockerfile 中没有定义 healthcheck，所以只能用 `condition: service_started`。但 backend 代码中有超时降级（`CompletableFuture.get(timeout)`），即使 AI 服务还没完全就绪，问诊请求也不会崩溃。

**面试官追问**：「如果 mysql 的 healthcheck 命令本身也失败了呢？（比如 mysqladmin 不存在）」——容器会被标记为 unhealthy，`docker-compose ps` 能看到 `unhealthy` 状态。backend 因为 `depends_on condition: service_healthy` 不会启动。排查方法：`docker logs health-mysql` 看启动日志。

---

### Q55. `X-Accel-Buffering: no` 这个头是干什么的？在哪里用的？

**标准回答**：

在 `diagnosis.py` 的 SSE 路由中：

```python
response.headers["X-Accel-Buffering"] = "no"
```

这是专门针对 **Nginx 反向代理**的配置。Nginx 默认有一个 `proxy_buffering on` 行为：当后端服务返回响应时，Nginx 会先把响应数据缓存到内存/磁盘，凑够了再发给客户端。对于普通 HTTP 请求这是好的（能优化 TCP 传输效率），但对于 SSE 流式输出是灾难——SSE 的逐帧推送被 Nginx 缓冲为一次性大块返回，破坏了流式体验。

`X-Accel-Buffering: no` 告诉 Nginx：「这条响应对应的 location 不要缓冲，后端 yield 一次就转发一次」。等效于 Nginx 配置：

```nginx
location /api/ai/diagnosis/stream {
    proxy_buffering off;
    proxy_cache off;
}
```

**面试官追问**：「Nginx 不缓冲了，性能有影响吗？」——SSE 本身就是长连接，一个连接持续 5-30 秒，Nginx 的长连接数有限（`worker_connections`）。如果 1000 个用户同时做 AI 问诊，需要 1000 个长连接。生产环境可以考虑：
1. Nginx 前面加一层边缘 CDN（如 Cloudflare）来分流长连接
2. 或者前端用短轮询（每 2 秒 GET /task/status）替代 SSE 长连接

---

### Q56. `Cache-Control: no-cache` 在 SSE 响应中的作用？

**标准回答**：

```python
response.headers["Cache-Control"] = "no-cache"
```

**`no-cache` 的真正含义**：很多人误解为「不缓存」，实际上它的含义是「每次使用前必须向服务器重新验证（revalidate）」。对于 SSE 场景的实际效果是：防止浏览器或中间代理缓存 SSE 事件流。

为什么不设成 `no-store`（彻底不缓存）？`no-store` 更强——任何中间节点（CDN、代理、浏览器）不得存储任何数据。对于 SSE 流来说 `no-cache` 已经足够——因为 SSE 是 `text/event-stream` MIME 类型，浏览器本身不会缓存这种响应。

**配套头部**：
```python
response.headers["Connection"] = "keep-alive"  # 保持长连接
response.headers["X-Accel-Buffering"] = "no"   # 禁用 Nginx 缓冲
response.headers["Cache-Control"] = "no-cache" # 禁用缓存
```

这三个头配合起来，确保 SSE 流式输出的实时性。

**面试官追问**：「如果漏掉了 Cache-Control，会发生什么？」——浏览器可能在开发过程中缓存了 SSE 响应（虽然罕见）。更常见的问题是中间 CDN/代理缓存，导致不同用户看到相同的流内容。加了 `no-cache` 是防御性的。

---

## 十、代码质量与工程实践

### Q57. 你用了 Lombok 哪些注解？@RequiredArgsConstructor 和 @AllArgsConstructor 有什么区别？Spring 推荐用哪个？

**标准回答**：

项目中用的 Lombok 注解：

| 注解 | 用途 | 示例 |
|------|------|------|
| `@Data` | Entity 类：自动生成 getter/setter/toString/equals/hashCode | `User`, `Appointment`, `Doctor` |
| `@Slf4j` | Service 类：自动生成 `log` 对象 | `ConsultationServiceImpl`, `AppointmentServiceImpl` |
| `@RequiredArgsConstructor` | Service/Controller/Config：生成 final 字段的构造器 | `SecurityConfig`, `JwtAuthenticationFilter` |

**@RequiredArgsConstructor vs @AllArgsConstructor**：

```java
// @RequiredArgsConstructor — 只对 final 字段和 @NonNull 字段生成构造器
@RequiredArgsConstructor
public class AppointmentServiceImpl {
    private final AppointmentMapper appointmentMapper;  // ✅ 加入构造器
    private final DoctorMapper doctorMapper;            // ✅ 加入构造器
    private RedisTemplate redisTemplate;                // ❌ 不加入（不是 final）
}

// @AllArgsConstructor — 对所有字段生成构造器（包括非 final）
@AllArgsConstructor
public class Something {
    private String a;  // ✅ 加入构造器
    private String b;  // ✅ 加入构造器
}
```

**Spring 推荐 @RequiredArgsConstructor**：原因是构造器注入比字段注入更好——
- `@Autowired private Foo foo;` — 字段注入，难以单测（需要反射或 Spring 容器）
- `private final Foo foo;` + `@RequiredArgsConstructor` — 构造器注入，单测可以直接 `new Service(mockFoo)`，而且 `final` 保证注入后不可变

**面试官追问**：「你项目里有混合使用 @Resource 和 @RequiredArgsConstructor 的情况吗？」——有。`ConsultationServiceImpl` 中 `@Resource(name = "aiExecutor") private Executor aiExecutor;` 用了 `@Resource` 字段注入，而其他依赖用了 `@RequiredArgsConstructor` 构造器注入。这是一个不够一致的写法——应该统一用构造器注入：`private final Executor aiExecutor;` 然后用 `@Qualifier("aiExecutor")` 注解构造器参数。

---

### Q58. 异常处理是怎么统一设计的？从 Controller 到 GlobalExceptionHandler 的完整链路是什么？

**标准回答**：

异常处理采用「统一响应格式 + 全局异常拦截」模式。

**Step 1 — 自定义业务异常**：
```java
public class BizException extends RuntimeException {
    private int code;
    private String message;
    
    public BizException(String message) { this(500, message); }
    public BizException(int code, String message) { ... }
}
```

**Step 2 — Service 层抛异常**：
```java
// 预约满了
throw new BizException(400, "该时段预约已满，请选择其他时段");

// 无权限
throw new BizException(403, "无权取消此预约");

// 不存在
throw new BizException(404, "预约记录不存在");
```

**Step 3 — GlobalExceptionHandler 统一拦截**：
```java
@RestControllerAdvice
public class GlobalExceptionHandler {

    // 业务异常（400/403/404）
    @ExceptionHandler(BizException.class)
    public Result<?> handleBizException(BizException e) {
        return Result.error(e.getCode(), e.getMessage());
    }

    // Spring Security 认证失败（401）
    @ExceptionHandler(AuthenticationException.class)
    public Result<?> handleAuthException(AuthenticationException e) {
        return Result.error(401, "未登录或登录已过期");
    }

    // Spring Security 权限不足（403）
    @ExceptionHandler(AccessDeniedException.class)
    public Result<?> handleAccessDenied(AccessDeniedException e) {
        return Result.error(403, "权限不足，无法访问此资源");
    }

    // 兜底：未捕获的异常（500）
    @ExceptionHandler(Exception.class)
    public Result<?> handleException(Exception e) {
        log.error("未捕获异常", e);
        return Result.error(500, "服务器内部错误");
    }
}
```

**Step 4 — 前端统一拦截**：
```typescript
// 在 axios/fetch 拦截器中
if (response.code === 401) {
    // 跳转登录页
    router.push('/login');
} else if (response.code === 403) {
    // 弹 toast 提示
    message.error('您没有权限执行此操作');
}
```

**面试官追问**：「@ExceptionHandler 和 SecurityConfig 中的 accessDeniedHandler 哪个先生效？」——SecurityConfig 中的 `accessDeniedHandler` 更早。它在 Security Filter Chain 中拦截 `AccessDeniedException`（Spring Security 抛的），在请求到达 Controller 之前。`@ExceptionHandler` 处理的是 Controller / Service 层抛的异常。两者是互补的——Filter 层的 403 由 accessDeniedHandler 处理，`@PreAuthorize` 注解触发的 403 也是由 accessDeniedHandler 处理（因为 `MethodSecurityInterceptor` 在 Filter Chain 中）。但如果你在 Service 层手写 `throw new BizException(403, ...)`，则由 `@ExceptionHandler` 处理。

---

### Q59. 登录接口的密码处理经历了什么变化？

**标准回答**：

**v1 版本（Hutool BCrypt）**：

```java
// 注册时
String encodedPassword = BCrypt.hashpw(password);  // 问题：没有加 salt，且方法调用可能不正确
// 登录时
boolean match = BCrypt.checkpw(password, user.getPassword());  // 这个调用是标准的
```

问题是：Hutool 的 BCrypt 和 Spring Security 的 BCrypt 实现细节可能略有差异（salt 格式、version 标记）。而且用 Hutool 意味着和 Spring Security 生态脱钩——没法享受 `PasswordEncoder` 接口带来的可替换性和测试便利性。

**v2 版本（Spring Security BCryptPasswordEncoder）**：

```java
// SecurityConfig 中注册 Bean
@Bean
public PasswordEncoder passwordEncoder() {
    return new BCryptPasswordEncoder();
}

// UserServiceImpl 中注入使用
private final PasswordEncoder passwordEncoder;

// 注册
user.setPasswordHash(passwordEncoder.encode(password));  // 自动生成随机 salt

// 登录
if (!passwordEncoder.matches(password, user.getPasswordHash())) {
    throw new BizException(400, "密码错误");
}
```

升级后带来的好处：
1. **接口抽象**：将来换 Argon2 → 只改 Bean 注入，`encode()`/`matches()` 调用方零修改
2. **标准实现**：Spring Security 的实现经过大量安全审计，比 Hutool 更可靠
3. **单测友好**：可以 `@MockBean PasswordEncoder` 做 mock

**面试官追问**：「从 Hutool 迁移到 Spring Security BCrypt 时，已有用户的密码怎么办？」——好消息是两种实现都遵循 BCrypt 标准格式：`$2a$10$...`（version + cost + salt + hash）。`BCryptPasswordEncoder.matches(raw, "$2a$10$...")` 能正确校验 Hutool 生成的 hash。所以迁移是平滑的——不需要用户重新设置密码。

---

### Q60. 如果有同事想在这个项目基础上加一个新业务模块（比如药品管理），应该怎么加？

**标准回答**：

按照本项目的约定，加新模块的标准步骤：

**Step 1 — 建表（数据库迁移）**：
```sql
-- 创建 database/migration_v4.sql
CREATE TABLE t_drug (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    drug_name VARCHAR(100) NOT NULL,
    generic_name VARCHAR(200),
    category VARCHAR(50),
    specification VARCHAR(100),
    manufacturer VARCHAR(200),
    price DECIMAL(10,2),
    status TINYINT DEFAULT 1,
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);
```

**Step 2 — 创建 Entity**：
```java
@Data
@TableName("t_drug")
public class Drug {
    @TableId(type = IdType.AUTO)
    private Long id;
    private String drugName;
    private String genericName;
    private String category;
    private BigDecimal price;
    private Integer status;
}
```

**Step 3 — 创建 Mapper**：
```java
@Mapper
public interface DrugMapper extends BaseMapper<Drug> {
    // 复杂查询写 XML
}
```

**Step 4 — 创建 DTO（如果需要区别 Entity 和请求/响应）**

**Step 5 — 创建 Service**：
```java
public interface DrugService {
    PageResult<Drug> pageList(String keyword, Long pageNum, Long pageSize);
    Drug create(DrugDTO dto);
    Drug update(Long id, DrugDTO dto);
}
```

**Step 6 — 创建 Controller + 权限注解**：
```java
@RestController
@RequestMapping("/api/medical/drugs")
public class DrugController {
    
    @GetMapping
    public Result<PageResult<Drug>> list(...) { }
    
    @PostMapping
    @PreAuthorize("hasRole('ADMIN')")  // 药品管理仅管理员
    public Result<Drug> create(@Valid @RequestBody DrugDTO dto) { }
}
```

**Step 7 — 更新 init.sql 和 Docker 部署脚本**

**面试官追问**：「如果药品表和处方表有外键关系，你会在 MySQL 中加 FOREIGN KEY 约束吗？」——会。预约 → 病历是逻辑外键（业务代码保证），但药品 → 处方是强数据一致性要求——处方里的药品不能是删除状态。用外键约束既保证数据完整，也让关联查询语义清晰。

---

## 十一、情景模拟题（面试官追问，区分「背答案的」和「真做过的」）

### Q61. 假设 AI 服务挂了，整个系统还能用吗？你是如何设计的？

**标准回答**：

能。本系统的架构设计保证了 **AI 问诊功能是可降级的，核心业务功能完全不依赖 AI**。

**核心业务功能（不受 AI 影响）**：
- 用户登录注册、科室查询、医生查询
- 预约挂号、确认/取消/完成
- 电子病历创建和查看
- 知识库管理

以上功能走 Java → MySQL/Redis，完全不调 Python AI 服务，AI 挂了照样正常运行。

**AI 问诊功能（多层降级）**：

```
AI 服务正常：
  → Redis 缓存命中？→ 直接返回（最快）
  → 缓存未命中 → 调 Python AI → 返回结果 → 写缓存

AI 服务异常（超时/报错）：
  → Java CompletableFuture.get(timeout) → TimeoutException
  → 降级处理：degrade(record, "AI 服务响应超时")
  → 返回：「抱歉，AI 预问诊服务暂时不可用...建议前往医院全科门诊就诊」

Python 自身异常：
  → 三层容错（结构化链路 → 容错解析 → 兜底建议）
```

**面试官追问**：「如果 Redis 也挂了，AI 缓存怎么办？」——AI 缓存不可用只是每轮都调 AI 服务（成本高），不会导致系统不可用。Redis 降级策略：

```java
try {
    Object cached = redisTemplate.opsForValue().get(cacheKey);
    // ...
} catch (Exception e) {
    log.warn("Redis 缓存不可用，直接调 AI");
    // 直接调 AI，不缓存
}
```

核心原则是：**缓存不应成为系统可用性的瓶颈**——缓存挂了就直通路，性能降级但不影响功能。

---

### Q62. 如果发现某用户用脚本暴力调用 AI 问诊接口（薅羊毛/消耗 API 费用），怎么防？

**标准回答**：

这是一个真实的安全/成本问题。DeepSeek API 按 token 计费，如果有人 24 小时暴力调用，每天可能产生上千元费用。

**防护方案（从简到严）**：

**1. 用户级限流（每日上限）**：
```java
// Redis Key: rate:user:{userId}:{date}
String key = "rate:user:" + userId + ":" + LocalDate.now();
Long count = redisTemplate.opsForValue().increment(key);
if (count == 1) redisTemplate.expire(key, 1, TimeUnit.DAYS);
if (count > 20) throw new BizException(429, "今日问诊次数已用完（20次/天）");
```

每个用户每天最多 20 次问诊。20 次对正常使用绰绰有余，对脚本来说是硬上限。

**2. IP 级限流（防止匿名调用）**：
```java
// Redis Key: rate:ip:{ip}:{minute}
// 滑动窗口：每个 IP 每分钟最多 5 次
String ipKey = "rate:ip:" + ip + ":" + now.getMinute();
// ... INCR + EXPIRE
```

**3. 问诊内容去重（防重复提交）**：
```java
String contentHash = DigestUtil.md5Hex(symptom);
String dedupKey = "dedup:user:" + userId + ":" + contentHash;
Boolean set = redisTemplate.opsForValue().setIfAbsent(dedupKey, "1", 60, TimeUnit.SECONDS);
if (!set) throw new BizException(429, "请勿重复提交相同内容");
```

60 秒内相同症状文本不允许重复提交。

**4. 接入 Sentinel 流量控制**（生产级）：
```java
@SentinelResource(value = "preDiagnosis", blockHandler = "rateLimitHandler")
public ConsultationVO preDiagnosis(...) { }
```

Sentinel 支持 QPS 限流（每秒最多 N 次问诊）、热点参数限流（高频症状单独限流）、系统自适应限流。

**面试官追问**：「这些限流放在哪一层？」——用户级限流在 Service 层（可以拿到 userId），IP 级限流在 Filter 层（可以拿到 request.getRemoteAddr）。层叠限流不能影响正常用户——正常用户一天 3-5 次问诊，限流阈值 20 远大于正常用量。

---

### Q63. 对话历史过长超出 LLM 上下文窗口怎么办？

**标准回答**：

DeepSeek 模型上下文窗口约 8K-32K tokens。假设用户连续问诊 30 轮，每轮 200 tokens → 6000 tokens + prompt（1000 tokens）+ 知识库（800 tokens）+ 输出预留（1000 tokens）≈ 8800 tokens，接近但未超出 8K 窗口。

但如果用户每轮描述都很长（如 500 字 ≈ 250 tokens），30 轮就是 15000 tokens，超出 8K。

**本项目已实现的两层策略**：

1. **滑动窗口**：只取最近 10 条消息（约 2000 tokens），旧的聊天记录不进 prompt
2. **摘要压缩**：消息数超 20 条时，注入 session summary（用压缩态描述代替原始对话）

**可以再升级的方案**：

**三级历史策略**：

```
最近 3 轮 — 保留完整原文（LLM 需要最新的上下文来理解当前追问）
第 4-10 轮 — 用摘要（保留关键信息但压缩 token）
第 11 轮以上 — 丢弃（已经太远，对当前追问几乎无帮助）
```

**Token 计数动态截断**（比固定条数更精准）：
```python
def truncate_history(history: str, max_tokens: int) -> str:
    # 粗略计算：中文 1 字 ≈ 0.4 token
    while estimate_tokens(history) > max_tokens:
        # 去掉最早的一条消息
        history = remove_earliest_message(history)
    return history
```

**面试官追问**：「你怎么估算 token 数？」——粗略估算：中文 1 字符 ≈ 0.4-0.5 token，英文 1 token ≈ 0.75 word。精确计算用 tokenizer（DeepSeek 的 tokenizer 开源，或者调 `tiktoken` 库做近似）。

---

### Q64. 电子病历涉及患者隐私数据，如果数据库被拖库，怎么保护？

**标准回答**：

这是一个考察安全意识的问题。防御分三层：

**第一层：传输安全**
- HTTPS 全链路加密（TLS 1.3），防止中间人窃听
- JWT token 过期时间短 + Refresh Token 轮换
- 前端 `localStorage` 存 token（不存 Cookie，防 CSRF）

**第二层：存储安全 — 敏感字段加密**（这是核心）：
```java
// 病历中的诊断和治疗方案是最高敏感度数据
// 存储前 AES-256-GCM 加密
record.setDiagnosis(aesEncrypt(dto.getDiagnosis()));
record.setTreatmentPlan(aesEncrypt(dto.getTreatmentPlan()));

// 查询时解密
record.setDiagnosis(aesDecrypt(record.getDiagnosis()));
```

关键设计：加密密钥不能和数据库在同一台服务器上。用 KMS（阿里云 KMS / HashiCorp Vault）管理密钥，应用启动时从 KMS 拉取，不写死在配置文件中。

**第三层：访问控制与审计**
- `MedicalRecordServiceImpl.detail()` 中做了角色归属校验（Q19）
- 数据库连接白名单（只允许后端服务器 IP 访问）
- 独立网段（数据库放在私有子网，不暴露公网端口）

**日志脱敏**：
```java
// 不要在日志中输出病历全文
log.info("病历创建成功: no={}, patient={}", record.getRecordNo(), record.getPatientId());
// ❌ 不要：log.info("诊断: {}", record.getDiagnosis());
```

**面试官追问**：「AES 加密后还能用 SQL LIKE 查询吗？」——不能。加密后的密文是无序的，`LIKE '%高血压%'` 失效。如果需要搜索，需要权衡：方案 1 — 用盲索引（Hash + Salt 后建索引，用于精确匹配）；方案 2 — 用同态加密（目前性能不足以在生产环境用）；方案 3 — 对不需要搜索的字段加密，需要搜索的字段（如科室名）不加密。

---

### Q65. 面试官可能会说：「你的问诊系统建议让患者去看发热门诊，但如果患者最后确诊是 COVID-19 重症，你会不会被告？」你怎么回答？

**标准回答**：

这是一个考察法律意识和产品责任边界的问题。

**第一：系统定位明确——「预问诊辅助」，不是「医疗诊断」**。

在产品的每个输出中都带免责声明：
```python
disclaimer="本建议由 AI 生成，仅供参考，不能替代执业医师诊断。"
```

在 Prompt 中也明确约束：
```
只提供「疑似」与「可能性」判断，绝不给出确定性诊断结论。
```

**第二：符合国家法规**。

根据《互联网诊疗管理办法（试行）》：
- 互联网诊疗活动应当由取得执业资质的医疗机构和医师开展
- AI 辅助工具属于「辅助诊断」，不能独立做出诊断结论
- 最终诊断权在执业医师，AI 输出只是参考信息

**第三：风险提示机制**。

在 Prompt 中强制要求急危重症筛查：
```
时刻警惕急危重症信号（剧烈胸痛、呼吸困难、意识障碍...），
一旦识别必须明确提示立即就医或拨打急救电话 120。
```

**第四：如果有执业医师复核环节会更安全**。

理想的产品架构是：AI 预问诊 → 生成初步建议 → 执业医师复核 → 确认/修改 → 发送给患者。这样医师承担最终责任，AI 只是提效工具。

**面试官追问**：「如果有人故意拿你的系统去免责（比如医生偷懒直接用 AI 建议开处方），怎么办？」——这是医疗 AI 产品的核心合规问题。需要在产品层面加「处方签权」机制——AI 建议不能直接生成处方，必须执业医师数字签名（U-Key / CA 证书）后才能生效。同时系统记录完整审计日志：AI 建议了什么、医生做了什么修改、最终处方是什么——出问题时可以追溯。

---

## 十二、开放性问题

### Q66. 如果让你把这个项目推到生产环境，你觉得最需要补什么？（按优先级排）

**标准回答**：

按优先级从高到低：

**P0（安全漏洞，必须修）**：
1. session 归属校验（Q28）——防止越权查看他人问诊历史
2. 预约并发竞态（Q48）——防止超挂号
3. 敏感数据加密（Q64）——病历诊断/treatment 字段 AES 加密存储
4. 接口限流（Q62）——防止暴力调用消耗 API 费用

**P1（稳定性）**：
5. 单元测试 + 集成测试 —— 至少覆盖核心 Service 方法（预约、病历、权限）
6. 日志采集 + 监控 —— ELK（日志） + Prometheus + Grafana（指标监控，如 AI 调用 P95 延迟）
7. SSL 证书 + HTTPS —— 全链路加密

**P2（规模化）**：
8. CI/CD 流水线 —— GitHub Actions 自动 build → test → deploy
9. 数据库读写分离 —— 问诊列表查询量大，走从库
10. AI 调用异步化 —— 不是同步 `future.get()`，而是返回 taskId + 前端轮询结果

**面试官追问**：「监控哪些指标？」——
- 业务指标：每日问诊量、预约量、AI 调用次数
- 性能指标：AI 调用 P50/P95/P99 延迟、API QPS
- 错误指标：AI 降级次数、401/403 错误率、5xx 错误率
- 资源指标：JVM 堆内存、MySQL 连接池、Redis 命中率

---

### Q67. 你做这个项目最大的收获是什么？

**标准回答**：

（建议结合自己的定位「Java + Python 大模型应用开发」来回答）

最大的收获是理解了 **LLM 应用 ≠ 调 API**。很多人觉得 AI 项目就是 `requests.post(openai_api, prompt)`，但真正落地的 LLM 应用是一整套工程体系：

1. **Prompt 工程**不只是写一句话——是 CoT 思维链设计、Few-shot 示例选择、多轮/单轮 prompt 分离、输出格式约束、容错解析。这些决定了 AI 回答的质量和系统稳定性。

2. **状态管理**是多轮对话的核心——怎么用两张表管理跨请求的上下文记忆，怎么做滑动窗口和摘要压缩控制 token 消耗。这和传统 CRUD 应用的「无状态 API」完全不同。

3. **安全不能只靠一层**——`@PreAuthorize` 注解 + Service 层数据范围过滤 + 业务归属校验，三层防御。LLM 应用的 token 成本也是安全问题（防滥用 = 防破产）。

4. **异构架构的打通**——Java 做业务和权限，Python 做 AI。不是学了 Java 就用 Java 写 AI，也不是学了 Python 就用 Python 写微服务。知道每种语言/生态的强项，然后把它们接在一起——这才是「LLM Application Engineer」的核心竞争力。

**面试官追问**：「如果让你重新做这个项目，你会怎么做不同的事？」——会从一开始就用 Spring Security 而非自定义拦截器（迁移成本），会先写单元测试再写业务代码（TDD），会先设计好 session 归属校验而不是后续补。

---

### Q68. 你在这个项目中最大的遗憾或未完成的功能是什么？

**标准回答**：

坦诚讲三个：

**1. 前端 SSE 流式展示没接上。** Python 端已经完整实现了 SSE 流式接口（首帧协议、token 逐帧推送），但前端当前用的是普通 POST 请求同步等结果。这意味着用户的「逐字输出」体验没有兑现。如果继续做，会用 fetch + ReadableStream 对接 SSE，实现类似 ChatGPT 的打字效果。

**2. 缺少单元测试。** 项目编译通过、功能可用，但没有 JUnit + Mockito 的测试用例。核心逻辑——如预约的状态转换、RBAC 数据范围过滤、JWT 签名验证——都应该有自动化测试保障。这在生产环境是最基本的质量保障。

**3. 多轮对话的摘要自动生成没有实现。** `diagnosis_session.summary` 字段预留了，ConsultationServiceImpl 中也做了 summary 注入逻辑，但实际的摘要生成（当消息超过 20 条时调用 AI 生成压缩版摘要）没有写。这是个典型的时间不够先留占位符的 trade-off。

**为什么坦诚说不足**：面试官更看重的是你知道问题在哪、知道怎么修。一个觉得自己项目完美无缺的候选人，要么没认真做，要么不知道自己不知道。

---

> - **总计：68 个问题，12 个维度，全面覆盖项目面试**
> - **准备优先级**：第二、四、五章（安全 + 多轮对话 + AI 集成）是核心差异化竞争力
> - **第十一章情景模拟**是区分「背答案的」和「真做过的」的决胜环节
> - **每个答案都应该能「带着代码讲的出来」**——面试官追问细节时，能立刻指出是哪个文件、哪个方法、几行代码
